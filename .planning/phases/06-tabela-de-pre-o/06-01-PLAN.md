---
phase: 06-tabela-de-pre-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["app/painel/precos/page.tsx", "app/painel/page.tsx"]
autonomous: true
requirements: [PRECO-01]
user_setup: []

must_haves:
  truths:
    - "Existe uma tela em /painel/precos mostrando redesign R$500–1.000 e manutenção R$97/mês, protegida pelo guard já existente em app/painel/layout.tsx (sem checagem de auth própria)"
    - "A dashboard principal (app/painel/page.tsx) linka pra essa tela e não mostra mais a mensagem desatualizada de 'próximas seções' pras fases já entregues"
  artifacts:
    - path: "app/painel/precos/page.tsx"
      provides: "Tela estática com a faixa de preço sugerida"
      contains: "R$500"
    - path: "app/painel/page.tsx"
      provides: "Card de acesso à tela de preços + remoção da mensagem de 'em construção' desatualizada"
      contains: "/painel/precos"
---

<objective>
PRECO-01: tela estática mostrando a faixa de preço sugerida (redesign R$500–1.000, manutenção R$97/mês) pro assinante usar ao vender pro cliente final. Última fase do milestone v1.0 — sem dependência de banco nem de nenhuma outra fase.

Aproveitando a edição de `app/painel/page.tsx`: a dashboard ainda mostra "🚧 Próximas seções: Redesenhar, Editor, Publicar, Proposta" — todas essas fases já foram entregues (Fases 2-5), então essa mensagem está desatualizada/incorreta. Corrigir isso junto (não é escopo novo, é consertar uma informação errada que fica ao lado do card novo sendo adicionado).

Output: `/painel/precos` (Server Component, protegido pelo guard de `app/painel/layout.tsx`) + dashboard atualizada.
</objective>

<context>
@.planning/phases/06-tabela-de-pre-o/06-CONTEXT.md
@app/painel/page.tsx
@app/painel/buscar/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar app/painel/precos/page.tsx</name>
  <files>app/painel/precos/page.tsx</files>
  <action>
Criar Server Component estático (sem "use client", sem query de banco -- os valores são fixos do requirement PRECO-01), seguindo o padrão visual de `app/painel/buscar/page.tsx` (mesmas classes `bg-bg`, `max-w-4xl`, `bg-card border border-border rounded-2xl`).

Conteúdo:
- Título "Tabela de Preço"
- Parágrafo curto de orientação (ex.: "Use como ponto de partida pra negociar com o cliente final -- ajuste conforme o porte do negócio.")
- Dois cards lado a lado (grid ou flex responsivo):
  - "Redesign" -- R$500 a R$1.000 -- breve descrição (ex.: "Cobrança única pela criação do novo site")
  - "Manutenção" -- R$97/mês -- breve descrição (ex.: "Recorrência mensal por manter o site no ar e atualizado")
- Link "← Painel" no topo, mesmo padrão do link "← Leads salvos"/"← Painel" já usado em outras páginas do painel.

Não precisa de guard de auth próprio -- `app/painel/layout.tsx` já protege toda a subárvore `/painel/*`.
  </action>
  <verify>grep -q "R\$500" "app/painel/precos/page.tsx" && grep -q "R\$97" "app/painel/precos/page.tsx"</verify>
  <done>Página estática criada, sem client state, sem query de banco, com os dois valores exatos do requirement.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar dashboard -- card de Preços + remover mensagem desatualizada</name>
  <files>app/painel/page.tsx</files>
  <action>
Em `app/painel/page.tsx`:

1. Adicionar um segundo card (mesmo estilo do card "Buscar" existente) linkando pra `/painel/precos`, com um ícone apropriado do `lucide-react` (ex. `DollarSign` ou `Tag`).
2. Substituir o bloco final:
```tsx
<div className="bg-card border border-border rounded-2xl p-8">
  <p className="text-muted text-sm">🚧 Próximas seções: Redesenhar, Editor, Publicar, Proposta.</p>
</div>
```
Por nada (remover), já que essas seções não são mais "próximas" -- todas as 6 fases do milestone já foram entregues (Buscar, Redesenhar, Editor, Publicar, Proposta, Preços) e cada uma é acessada a partir do fluxo de leads salvos (Buscar → salvar lead → Redesenhar/Editor/Publicar/Proposta ficam na página do lead) ou do novo card de Preços.
  </action>
  <verify>grep -q "/painel/precos" "app/painel/page.tsx" && ! grep -q "Próximas seções" "app/painel/page.tsx" && npm run build</verify>
  <done>Dashboard linka pra /painel/precos, mensagem desatualizada removida, build compila.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `/painel/precos` mostra R$500–1.000 (redesign) e R$97/mês (manutenção)
- [ ] `/painel/precos` está protegida pelo guard existente (não acessível sem sessão/assinatura ativa -- herdado de app/painel/layout.tsx, sem código novo de auth)
- [ ] Dashboard (`app/painel/page.tsx`) linka pra `/painel/precos` e não mostra mais a mensagem de "em construção"
</verification>

<success_criteria>
- Requirement PRECO-01 completo
- Build limpo
- Milestone v1.0 completo (todas as 6 fases do roadmap entregues)
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-tabela-de-pre-o/06-01-SUMMARY.md`
</output>
