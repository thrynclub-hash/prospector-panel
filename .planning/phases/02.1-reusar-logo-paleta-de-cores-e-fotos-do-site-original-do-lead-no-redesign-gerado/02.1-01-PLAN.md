---
phase: 02.1-reusar-logo-paleta-de-cores-e-fotos-do-site-original-do-lead-no-redesign-gerado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [types/redesign-content.ts, lib/screenshot.ts, lib/site-scrape.ts, app/api/redesigns/generate/route.ts, "app/api/redesigns/[id]/route.ts", "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"]
autonomous: true
requirements: [REDESENHAR-05]
user_setup: []

must_haves:
  truths:
    - "theme é uma chave-irmã de nível superior em RedesignContent (opcional/nullable), nunca aninhada dentro de photos -- redesigns antigos sem essa chave continuam satisfazendo o tipo"
    - "PATCH em app/api/redesigns/[id]/route.ts propaga theme sempre a partir de currentContent.theme, nunca aceito do body da requisição -- mesmo padrão read-only já usado por facts"
    - "O scraping do site original (screenshot/logo via Microlink, fotos/theme-color via fetch+regex) roda inteiramente FORA do bloco try/catch que hoje devolve 502 em generate/route.ts -- nunca pode bloquear ou fazer a geração falhar"
    - "Quando o site original não tem logo/paleta/fotos extraíveis, ou lead.has_own_website é false, o redesign gerado cai exatamente no comportamento de hoje: template neutro + fallback tipográfico de logo + fotos do Places"
    - "Fotos extraídas do site original são preferidas às fotos do Places quando disponíveis, sempre re-hospedadas via uploadFromUrl (nunca linkadas direto do site do lead)"
    - "redesign-preview.tsx usa content.theme?.primaryColor como cor de destaque (fundo do hero, títulos) quando presente, mantendo o template neutro atual quando ausente -- consumido tanto no preview privado quanto em app/demo/[slug] (mesmo componente)"
  artifacts:
    - path: "types/redesign-content.ts"
      provides: "Campo `theme?: { primaryColor: string | null } | null` como chave-irmã de RedesignContent"
      contains: "theme?:"
    - path: "lib/screenshot.ts"
      provides: "captureSiteVisuals() substitui captureScreenshot(), retorna { screenshotUrl, logoUrl } numa única chamada Microlink com meta=true"
      contains: "export async function captureSiteVisuals"
    - path: "lib/site-scrape.ts"
      provides: "scrapeSiteAssets() extrai fotos (regex <img>, heurística de tamanho/nome, cap 3) + theme-color (regex <meta theme-color>) de um fetch só, nunca lança"
      contains: "export async function scrapeSiteAssets"
    - path: "app/api/redesigns/generate/route.ts"
      provides: "Bloco de extração roda depois do try/catch de erro; re-hospeda logo/fotos scraped via uploadFromUrl; content.theme populado; fallback pra Places Photos preservado"
      contains: "scrapeSiteAssets"
    - path: "app/api/redesigns/[id]/route.ts"
      provides: "PATCH thread theme: currentContent.theme (read-only, mesmo padrão de facts)"
      contains: "theme: currentContent.theme"
    - path: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      provides: "Renderiza theme?.primaryColor como cor de destaque no hero/títulos"
      contains: "theme?.primaryColor"
  key_links:
    - from: "app/api/redesigns/generate/route.ts"
      to: "lib/site-scrape.ts"
      via: "scrapeSiteAssets(websiteUri) chamado fora do try/catch de erro, resultado usado pra theme e fallback de fotos"
      pattern: "scrapeSiteAssets\\(websiteUri\\)"
    - from: "app/api/redesigns/generate/route.ts"
      to: "lib/storage/assets.ts"
      via: "uploadFromUrl() reaproveitado (já existia, sem uso) pra re-hospedar logo/fotos scraped do site do lead"
      pattern: "uploadFromUrl\\(supabase"
    - from: "app/api/redesigns/[id]/route.ts"
      to: "types/redesign-content.ts"
      via: "theme threaded exatamente como facts -- nunca aceito de body.theme (não existe no tipo do body)"
      pattern: "theme: currentContent\\.theme"
    - from: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      to: "types/redesign-content.ts"
      via: "content.theme?.primaryColor consumido como accent color, com fallback pro template neutro atual"
      pattern: "theme\\?\\.primaryColor"
---

<objective>
02.1-RESEARCH.md identificou que o redesign gerado (Fase 2) nunca reaproveita logo/paleta/fotos do site ORIGINAL do lead -- só fotos do Google Places + template neutro. Confirmado num teste manual real (lead "Dra. Tania Higaki"): o redesign usou foto de estoque genérica sem relação com o negócio. Esta fase fecha esse gap.

**Mecanismo de extração** (02.1-RESEARCH.md, "TL;DR"):
1. **Logo**: extensão da chamada Microlink já existente em `lib/screenshot.ts` com `&meta=true` -- Microlink já resolve um campo `logo` normalizado (favicon/apple-touch-icon/og:image). Zero nova lib, zero round-trip extra.
2. **Fotos (galeria)**: novo scrape leve (`lib/site-scrape.ts`) -- fetch simples do HTML + regex `<img>`, seguindo exatamente o padrão já estabelecido em `lib/email-scrape.ts` (timeout, User-Agent, try/catch → vazio, nunca lança). Sem parser de HTML instalado no projeto -- regex é consistente com a convenção existente.
3. **Paleta de cores**: só o tier primário -- regex de `<meta name="theme-color">` no mesmo HTML já buscado pras fotos. **O fallback via `sharp`/dominant-color-do-logo (mencionado como tier secundário em RESEARCH.md) fica DEFERIDO** -- a própria pesquisa marca isso como "open item, not blocking" ("theme-color meta tag alone may already cover enough cases to be worth shipping first, with the sharp logo-quantization fallback as a fast-follow"). Implementá-lo exigiria ou uma nova dependência (`sharp`, não declarada em `package.json` hoje) ou um fetch duplicado do logo pra obter os bytes brutos (já que `uploadFromUrl` -- que este plano é obrigado a reaproveitar sem criar novo código de re-hosting -- não expõe os bytes de volta ao chamador). Decisão: ficar só com `theme-color` no v1; quando ausente, `theme` fica `null` e cai no template neutro de hoje -- exatamente o comportamento exigido pelo CONTEXT.md ("nunca quebra a geração").
4. **Schema**: `theme` entra como chave-irmã de nível superior em `RedesignContent` (nunca aninhada em `photos` -- ver "Critical finding" do RESEARCH.md: o PATCH de `app/api/redesigns/[id]/route.ts` faz `photos: body.photos ?? currentContent.photos`, substituição integral, e `editor-form.tsx` sempre manda um `photos` reconstruído à mão -- qualquer chave nova dentro de `photos` seria apagada no primeiro "Salvar edição"). Sem migração SQL: `redesigns.content` é `jsonb` sem CHECK constraint, e o campo é opcional -- redesigns antigos sem a chave continuam válidos.
5. **Sem AI Gateway**: extração pura, roda em paralelo à `generateRedesignCopy()` já existente, não a substitui.

**Escopo confirmado (CONTEXT.md, locked)**: só redesigns gerados daqui pra frente -- sem backfill/reprocessamento de linhas existentes (mesmo padrão já usado pro `facts.phone` na Fase 5). Como não há migração SQL nem coluna nova, não há nada pra "rodar" contra dados antigos -- eles simplesmente continuam sem a chave `theme`, lida como `undefined` por código que já trata o campo como opcional.

Purpose: fechar REDESENHAR-05 sem quebrar o freeze de `RedesignContent`, sem tornar a geração menos confiável, e sem re-hospedar imagem de terceiro sem re-upload pro Storage.
Output: extração de logo/fotos/cor do site original, non-blocking, com fallback gracioso; `theme` consumido de fato no preview (privado e público, mesmo componente).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-reusar-logo-paleta-de-cores-e-fotos-do-site-original-do-lead-no-redesign-gerado/02.1-CONTEXT.md
@.planning/phases/02.1-reusar-logo-paleta-de-cores-e-fotos-do-site-original-do-lead-no-redesign-gerado/02.1-RESEARCH.md
@types/redesign-content.ts
@app/api/redesigns/generate/route.ts
@app/api/redesigns/[id]/route.ts
@lib/screenshot.ts
@lib/email-scrape.ts
@lib/storage/assets.ts
@app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx
@app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar `theme` como chave-irmã opcional em RedesignContent</name>
  <files>types/redesign-content.ts</files>
  <action>
Editar `types/redesign-content.ts`. Adicionar um novo campo `theme` de nível superior, opcional e nullable, e um comentário explicando por que não pode ir dentro de `photos`:

```ts
// Shape CONGELADO de redesigns.content (ARCHITECTURE.md Anti-Pattern 3).
// Editor (Fase 3) e Publicar (Fase 4) leem/escrevem exatamente este shape --
// mudar isso depois exige migração de dados, não só código.
//
// REDESENHAR-02: separação explícita entre `facts` (verificado, nunca
// reescrito pela IA) e `generated` (copy de marketing, pode ser regenerado).
// Nenhum campo de horário/preço/certificação/depoimento existe aqui de
// propósito -- omitir é a resposta correta quando a fonte não tem o dado
// (skill redesign-premium, regra 1; Pitfall 2).
//
// REDESENHAR-05 (Fase 02.1): `theme` é uma extensão ADITIVA -- chave-irmã de
// nível superior, opcional/nullable de propósito (02.1-RESEARCH.md "Critical
// finding"). NUNCA aninhar isso dentro de `photos`: o PATCH em
// app/api/redesigns/[id]/route.ts faz `photos: body.photos ?? currentContent.photos`
// (substituição integral, não merge) e editor-form.tsx sempre manda um
// `photos` reconstruído à mão com só logoUrl/photoUrls -- qualquer chave nova
// dentro de `photos` seria apagada no primeiro "Salvar edição". `theme`
// segue o mesmo padrão já usado por `facts`: escrito uma vez na geração,
// read-only depois (nunca aceito do body do PATCH). Redesigns gerados antes
// desta fase não têm essa chave -- por isso é opcional, não só nullable.
export interface RedesignContent {
  facts: {
    name: string;
    category: string | null;
    address: string | null;
    phone: string | null;
    websiteUrl: string | null;
    rating: number | null;
    userRatingCount: number | null;
  };
  generated: {
    heroHeadline: string;
    heroSubheadline: string;
    aboutCopy: string;
    services: Array<{ title: string; description: string }>;
  };
  photos: {
    logoUrl: string | null;
    photoUrls: string[];
  };
  theme?: {
    primaryColor: string | null;
  } | null;
}
```
  </action>
  <verify>grep -q "theme?:" types/redesign-content.ts</verify>
  <done>Campo theme adicionado como chave-irmã opcional/nullable, com comentário explicando a restrição de não aninhar em photos.</done>
</task>

<task type="auto">
  <name>Task 2: Estender lib/screenshot.ts pra também capturar o logo (mesma chamada Microlink)</name>
  <files>lib/screenshot.ts</files>
  <action>
Substituir todo o conteúdo de `lib/screenshot.ts`. `captureScreenshot` vira `captureSiteVisuals`, que acrescenta `&meta=true` à mesma URL da Microlink e lê tanto `screenshot.url` quanto `logo.url` da resposta -- confirmado ao vivo contra stripe.com em 02.1-RESEARCH.md (retornou o favicon 180x180). É a única função deste arquivo e o único caller é `app/api/redesigns/generate/route.ts` (atualizado na Task 4 deste mesmo plano), então renomear é seguro.

```ts
// Screenshot do site original pro comparador antes/depois (REDESENHAR-03) +
// logo do negócio pro redesign (REDESENHAR-05) -- uma chamada só à Microlink
// (API gratuita, sem chave). meta=true acrescenta metadata normalizada, que
// já inclui um campo `logo` resolvido via a cadeia padrão
// apple-touch-icon/shortcut-icon/favicon -- confirmado ao vivo contra
// stripe.com (02.1-RESEARCH.md, retornou o favicon 180x180 PNG deles). Zero
// nova lib, zero round-trip HTTP extra: mesma chamada que já existia pro
// screenshot "antes".
//
// Ver STACK.md "What NOT to Use": Chromium num Vercel Function estoura o
// limite de tamanho e tem cold start de 3-8s. Se o volume crescer, trocar
// por um provedor pago (ScreenshotOne/urlbox) é só trocar a URL desta função.
export async function captureSiteVisuals(
  url: string
): Promise<{ screenshotUrl: string | null; logoUrl: string | null }> {
  try {
    const res = await fetch(
      `https://api.microlink.io/?url=${encodeURIComponent(url)}&screenshot=true&meta=true&waitUntil=networkidle2`,
      { signal: AbortSignal.timeout(15_000) }
    );
    if (!res.ok) return { screenshotUrl: null, logoUrl: null };

    const data = (await res.json()) as {
      status: string;
      data?: { screenshot?: { url?: string }; logo?: { url?: string } };
    };
    if (data.status !== "success") return { screenshotUrl: null, logoUrl: null };

    return {
      screenshotUrl: data.data?.screenshot?.url ?? null,
      logoUrl: data.data?.logo?.url ?? null,
    };
  } catch {
    // Site fora do ar, timeout, rate limit da Microlink -- "sem
    // screenshot/logo", não erro fatal da geração inteira (REDESENHAR-03/05
    // degradam bem sem isso).
    return { screenshotUrl: null, logoUrl: null };
  }
}
```
  </action>
  <verify>grep -q "export async function captureSiteVisuals" lib/screenshot.ts</verify>
  <done>captureSiteVisuals criada, única chamada Microlink com meta=true, retorna screenshotUrl e logoUrl. Nota: o caller em app/api/redesigns/generate/route.ts ainda referencia o nome antigo `captureScreenshot` neste ponto -- só é corrigido na Task 4; build completo só é esperado após ela.</done>
</task>

<task type="auto">
  <name>Task 3: Criar lib/site-scrape.ts (fotos + theme-color, um fetch só)</name>
  <files>lib/site-scrape.ts</files>
  <action>
Criar `lib/site-scrape.ts`, seguindo exatamente o padrão de `lib/email-scrape.ts` (fetch com timeout + User-Agent, regex, try/catch → vazio, nunca lança). Um fetch só alimenta tanto a extração de fotos quanto a de theme-color:

```ts
// Extração best-effort de fotos e cor de marca do site original do lead
// (REDESENHAR-05) -- mesmo padrão de lib/email-scrape.ts: fetch com timeout,
// regex (sem parser de HTML instalado no projeto), try/catch -> valor vazio,
// nunca lança. Um scrape "sem sucesso" é resultado esperado, não erro --
// quem chama isso (generate/route.ts) já trata null/[] como "cai no
// fallback de hoje: template neutro + fotos do Places".
const IMG_TAG_RE = /<img\b[^>]*>/gi;
const SRC_RE = /\bsrc=["']([^"']+)["']/i;
const WIDTH_RE = /\bwidth=["']?(\d+)/i;
const HEIGHT_RE = /\bheight=["']?(\d+)/i;
const THEME_COLOR_RE = /<meta[^>]+name=["']theme-color["'][^>]*content=["']([^"']+)["']/i;

const IGNORED_FILENAME_PATTERNS = ["icon", "sprite", "favicon", "logo", "pixel", "1x1", "spacer", "avatar"];
const MIN_DIMENSION = 150;
const MAX_PHOTOS = 3;

function isLikelyContentImage(imgTag: string, src: string): boolean {
  if (src.startsWith("data:")) return false;

  const lowerSrc = src.toLowerCase();
  if (IGNORED_FILENAME_PATTERNS.some((pattern) => lowerSrc.includes(pattern))) return false;

  // Rejeita por dimensão declarada só quando o atributo existe -- muitas
  // imagens de conteúdo real não anotam width/height, então ausência não é
  // motivo de rejeição, só presença de um valor pequeno.
  const width = Number(imgTag.match(WIDTH_RE)?.[1]);
  const height = Number(imgTag.match(HEIGHT_RE)?.[1]);
  if (width && width < MIN_DIMENSION) return false;
  if (height && height < MIN_DIMENSION) return false;

  return true;
}

function resolveUrl(src: string, baseUrl: string): string | null {
  try {
    return new URL(src, baseUrl).toString();
  } catch {
    return null;
  }
}

export async function scrapeSiteAssets(
  websiteUrl: string
): Promise<{ photoUrls: string[]; themeColor: string | null }> {
  try {
    const res = await fetch(websiteUrl, {
      signal: AbortSignal.timeout(8_000),
      headers: { "User-Agent": "Mozilla/5.0 (compatible; HunterOfBadPages/1.0)" },
    });
    if (!res.ok) return { photoUrls: [], themeColor: null };

    const html = await res.text();

    const photoUrls: string[] = [];
    for (const imgTag of html.match(IMG_TAG_RE) ?? []) {
      if (photoUrls.length >= MAX_PHOTOS) break;
      const src = imgTag.match(SRC_RE)?.[1];
      if (!src || !isLikelyContentImage(imgTag, src)) continue;
      const resolved = resolveUrl(src, websiteUrl);
      if (resolved && !photoUrls.includes(resolved)) photoUrls.push(resolved);
    }

    const themeColor = html.match(THEME_COLOR_RE)?.[1] ?? null;

    return { photoUrls, themeColor };
  } catch {
    // Site fora do ar, timeout, bloqueio de scraping (CORS/anti-bot não
    // afeta fetch server-side, mas WAFs podem recusar) -- "não achou", não
    // erro fatal.
    return { photoUrls: [], themeColor: null };
  }
}
```

Nota: sem parser de HTML instalado no projeto (`cheerio`/`linkedom`/etc. não estão em `package.json`) -- regex é consistente com a convenção já usada por `lib/email-scrape.ts` e suficiente para uma heurística best-effort (02.1-RESEARCH.md).
  </action>
  <verify>grep -q "export async function scrapeSiteAssets" lib/site-scrape.ts</verify>
  <done>Função criada, único fetch, extrai fotos (cap 3, heurística de nome/tamanho) e theme-color, nunca lança.</done>
</task>

<task type="auto">
  <name>Task 4: Wire -- generate/route.ts extrai e re-hospeda logo/fotos/cor, fora do try/catch de erro</name>
  <files>app/api/redesigns/generate/route.ts</files>
  <action>
Substituir todo o conteúdo de `app/api/redesigns/generate/route.ts`. Mudanças-chave em relação ao arquivo atual:

1. Import `uploadFromUrl` (já existe em `lib/storage/assets.ts`, sem uso hoje) ao lado de `uploadAsset`; trocar `captureScreenshot` por `captureSiteVisuals`; importar `scrapeSiteAssets` de `lib/site-scrape`.
2. Dentro do try/catch existente (que já cobre Places + AI Gateway + Storage, e já devolve 502 em JSON no catch): manter exatamente a lógica de hoje (fetch de `details`, fotos do Places re-hospedadas, `generateRedesignCopy`), só reorganizando pra guardar `facts`/`generated`/`placesPhotoUrls`/`websiteUri` em variáveis declaradas fora do try (mesmo padrão já usado hoje por `content`/`beforeScreenshotUrl` no arquivo original -- `let` fora, atribuído dentro do try, lido depois porque o catch sempre retorna).
3. DEPOIS do try/catch (fora dele, de propósito -- 02.1-RESEARCH.md "Failure/fallback behavior"): chamar `captureSiteVisuals` e `scrapeSiteAssets`, re-hospedar o que vier via `uploadFromUrl`, e montar `theme`. Esse bloco não pode nunca fazer a rota retornar 502 -- cada função chamada aqui já degrada sozinha pra `null`/`[]`.
4. Fotos do site original substituem as do Places quando houver pelo menos uma extraída com sucesso (CONTEXT.md: "o site original passa a ser preferido quando disponível"); caso contrário mantém as fotos do Places já re-hospedadas (comportamento de hoje, inalterado).

```ts
import { NextResponse } from "next/server";
import { requireActiveUser } from "@/lib/auth-guard";
import { checkQuota, recordUsage } from "@/lib/quota";
import { createGooglePlacesClient } from "@/lib/google-places/client";
import { uploadAsset, uploadFromUrl } from "@/lib/storage/assets";
import { captureSiteVisuals } from "@/lib/screenshot";
import { scrapeSiteAssets } from "@/lib/site-scrape";
import { generateRedesignCopy } from "@/lib/ai/generate-redesign";
import type { RedesignContent } from "@/types/redesign-content";

export async function POST(request: Request) {
  const guard = await requireActiveUser();
  if ("error" in guard) return guard.error;
  const { supabase, user } = guard;

  const { leadId } = (await request.json().catch(() => ({}))) as { leadId?: string };
  if (!leadId) {
    return NextResponse.json({ error: "leadId obrigatório" }, { status: 400 });
  }

  const { data: lead, error: leadError } = await supabase
    .from("leads")
    .select("id, place_id, has_own_website, public_email")
    .eq("id", leadId)
    .eq("user_id", user.id)
    .maybeSingle();

  if (leadError || !lead) {
    return NextResponse.json({ error: "Lead não encontrado" }, { status: 404 });
  }

  const quota = await checkQuota(supabase, user.id, "redesign_generate");
  if (!quota.allowed) {
    return NextResponse.json(
      { error: "Limite diário de gerações atingido", used: quota.used, limit: quota.limit },
      { status: 429 }
    );
  }

  const placesClient = createGooglePlacesClient();
  let placesPhotoUrls: string[];
  let websiteUri: string | null;
  let facts: RedesignContent["facts"];
  let generated: RedesignContent["generated"];

  try {
    const details = await placesClient.getDetails(lead.place_id);
    if (!details) {
      return NextResponse.json({ error: "Negócio não encontrado no Places (place_id inválido/expirado)" }, { status: 404 });
    }

    // Fotos: re-hospedadas no Storage -- nunca a URL de mídia do Places
    // direto (vazaria a API key pro navegador quando isto for exibido
    // publicamente na Fase 4). Continuam sendo o FALLBACK quando o site
    // original não tiver fotos aproveitáveis (REDESENHAR-05) -- ver bloco
    // não-bloqueante logo abaixo do try/catch.
    const photoNames = (details.photos ?? []).slice(0, 3).map((p) => p.name);
    placesPhotoUrls = [];
    for (const photoName of photoNames) {
      const photo = await placesClient.fetchPhotoBytes(photoName).catch(() => null);
      if (!photo) continue;
      const ext = photo.contentType.includes("png") ? "png" : "jpg";
      const url = await uploadAsset(supabase, {
        userId: user.id,
        path: `${leadId}/photo-${placesPhotoUrls.length}.${ext}`,
        bytes: photo.bytes,
        contentType: photo.contentType,
      }).catch(() => null);
      if (url) placesPhotoUrls.push(url);
    }

    websiteUri = details.websiteUri ?? null;

    generated = await generateRedesignCopy({
      name: details.displayName?.text ?? "(sem nome)",
      category: details.primaryType ?? null,
      address: details.formattedAddress ?? null,
      rating: details.rating ?? null,
      userRatingCount: details.userRatingCount ?? null,
      badSiteReason: lead.has_own_website ? "Site próprio com problemas de qualidade/performance" : "Sem site próprio",
    });

    facts = {
      name: details.displayName?.text ?? "(sem nome)",
      category: details.primaryType ?? null,
      address: details.formattedAddress ?? null,
      phone: details.internationalPhoneNumber ?? null,
      websiteUrl: details.websiteUri ?? null,
      rating: details.rating ?? null,
      userRatingCount: details.userRatingCount ?? null,
    };
  } catch (err) {
    // Erros daqui (Places, AI Gateway, Storage) não podem virar um 500 HTML
    // sem corpo JSON -- o cliente não consegue mostrar a mensagem real e cai
    // no fallback genérico "Erro de conexão" (foi exatamente o que aconteceu
    // com o erro de cartão de crédito do AI Gateway).
    const message = err instanceof Error ? err.message : String(err);
    console.error("redesigns/generate: erro na geração", err);
    return NextResponse.json({ error: `Geração falhou: ${message}` }, { status: 502 });
  }

  // REDESENHAR-05: extração do site original (screenshot, logo, fotos, cor
  // de marca) roda FORA do try/catch acima de propósito -- é estritamente
  // aditiva e nunca deve virar um 502 de geração (02.1-RESEARCH.md
  // "Failure/fallback behavior"). Cada chamada já degrada sozinha pra
  // null/[] em qualquer falha (mesmo padrão de lib/email-scrape.ts) -- um
  // site que bloqueia scraping, não tem logo, ou não declara theme-color cai
  // exatamente no comportamento de hoje: template neutro + fotos do Places.
  let beforeScreenshotUrl: string | null = null;
  let finalLogoUrl: string | null = null;
  let finalPhotoUrls = placesPhotoUrls;
  let theme: RedesignContent["theme"] = null;

  if (lead.has_own_website && websiteUri) {
    const siteVisuals = await captureSiteVisuals(websiteUri);
    beforeScreenshotUrl = siteVisuals.screenshotUrl;

    const scraped = await scrapeSiteAssets(websiteUri);

    // Site original é preferido às fotos do Places quando dá pra extrair
    // fotos aproveitáveis (CONTEXT.md) -- re-hospedadas exatamente como as
    // fotos do Places, nunca linkadas direto do site do lead.
    if (scraped.photoUrls.length > 0) {
      const rehosted: string[] = [];
      for (const [index, sourceUrl] of scraped.photoUrls.entries()) {
        const url = await uploadFromUrl(supabase, {
          userId: user.id,
          path: `${leadId}/site-photo-${index}.jpg`,
          sourceUrl,
        });
        if (url) rehosted.push(url);
      }
      if (rehosted.length > 0) finalPhotoUrls = rehosted;
    }

    if (siteVisuals.logoUrl) {
      finalLogoUrl = await uploadFromUrl(supabase, {
        userId: user.id,
        path: `${leadId}/logo.png`,
        sourceUrl: siteVisuals.logoUrl,
      });
    }

    if (scraped.themeColor) {
      theme = { primaryColor: scraped.themeColor };
    }
  }

  const content: RedesignContent = {
    facts,
    generated,
    photos: { logoUrl: finalLogoUrl, photoUrls: finalPhotoUrls },
    theme,
  };

  await recordUsage(supabase, user.id, "redesign_generate");

  const { data: redesign, error: insertError } = await supabase
    .from("redesigns")
    .insert({
      user_id: user.id,
      lead_id: leadId,
      content,
      before_screenshot_url: beforeScreenshotUrl,
      status: "ready",
    })
    .select("id, content, before_screenshot_url, status, created_at")
    .single();

  if (insertError) {
    console.error("redesigns/generate: erro salvando redesign", insertError);
    return NextResponse.json({ error: "Erro salvando redesign" }, { status: 500 });
  }

  await supabase.from("leads").update({ status: "redesigned" }).eq("id", leadId).eq("user_id", user.id);

  return NextResponse.json({ redesign, quota: { used: quota.used + 1, limit: quota.limit } }, { status: 201 });
}
```
  </action>
  <verify>grep -q "scrapeSiteAssets" app/api/redesigns/generate/route.ts && grep -q "captureSiteVisuals" app/api/redesigns/generate/route.ts && grep -q "uploadFromUrl" app/api/redesigns/generate/route.ts</verify>
  <done>Rota importa e usa as três novas peças; extração roda depois do try/catch de erro; fotos do site preferidas às do Places quando disponíveis; theme populado ou null.</done>
</task>

<task type="auto">
  <name>Task 5: PATCH -- thread theme como read-only, mesmo padrão de facts</name>
  <files>app/api/redesigns/[id]/route.ts</files>
  <action>
Em `app/api/redesigns/[id]/route.ts`, no handler `PATCH`, dentro da construção de `updatedContent` (hoje):

```ts
  const updatedContent: RedesignContent = {
    facts: currentContent.facts,
    generated: body.generated ?? currentContent.generated,
    photos: body.photos ?? currentContent.photos,
  };
```

Adicionar a linha `theme: currentContent.theme,` (sempre herdado do valor já salvo, nunca do body -- não existe (nem deve existir) `theme` no tipo do body desta rota, então não há como o cliente sobrescrever isso mesmo por engano):

```ts
  const updatedContent: RedesignContent = {
    facts: currentContent.facts,
    generated: body.generated ?? currentContent.generated,
    photos: body.photos ?? currentContent.photos,
    theme: currentContent.theme,
  };
```

Nenhuma outra mudança nesta rota -- não adicionar `theme` ao tipo do `body` desestruturado no topo do handler (não existe UI para editar cor; deve permanecer inteiramente fora do alcance do cliente).
  </action>
  <verify>grep -q "theme: currentContent.theme" "app/api/redesigns/[id]/route.ts"</verify>
  <done>PATCH propaga theme sempre a partir do valor salvo, nunca do body da requisição.</done>
</task>

<task type="auto">
  <name>Task 6: redesign-preview.tsx consome theme.primaryColor como accent</name>
  <files>app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx</files>
  <action>
Substituir todo o conteúdo de `app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx`. Quando `content.theme?.primaryColor` existir, usar como cor de fundo do hero e cor dos títulos "Sobre"/"Serviços" (via `style` inline, já que é uma cor dinâmica vinda de dado, não uma classe Tailwind estática); quando ausente, manter exatamente o template neutro atual (`bg-slate-900`/`text-slate-300`). Este componente é o renderizador compartilhado do preview privado E de `app/demo/[slug]` -- uma mudança aqui aparece nos dois lugares automaticamente, sem lógica duplicada.

```tsx
import type { RedesignContent } from "@/types/redesign-content";

// Preview do redesign gerado. Quando o site original tem uma cor de marca
// extraída (REDESENHAR-05, content.theme.primaryColor), ela é usada como
// destaque (fundo do hero, títulos) em vez do template neutro -- que é a
// marca do PAINEL, não do negócio do lead (skill redesign-premium regra 3).
// Sem cor extraída (site não declarou theme-color, ou não tem site próprio),
// cai exatamente no template neutro de sempre. Componente compartilhado pelo
// preview privado e por app/demo/[slug] -- uma única fonte de renderização.
export function RedesignPreview({ content }: { content: RedesignContent }) {
  const accentColor = content.theme?.primaryColor ?? null;

  return (
    <div className="rounded-2xl overflow-hidden border border-border bg-white text-slate-900">
      <div
        className={accentColor ? "px-8 py-16 text-center text-white" : "px-8 py-16 text-center bg-slate-900 text-white"}
        style={accentColor ? { backgroundColor: accentColor } : undefined}
      >
        {content.photos.logoUrl ? (
          // eslint-disable-next-line @next/next/no-img-element
          <img src={content.photos.logoUrl} alt={content.facts.name} className="h-12 mx-auto mb-6 rounded" />
        ) : (
          <div className="w-14 h-14 mx-auto mb-6 rounded-full bg-white/10 flex items-center justify-center text-xl font-bold">
            {content.facts.name.slice(0, 1).toUpperCase()}
          </div>
        )}
        <h1 className="text-3xl font-bold mb-3">{content.generated.heroHeadline}</h1>
        <p className={accentColor ? "opacity-90 max-w-lg mx-auto" : "text-slate-300 max-w-lg mx-auto"}>
          {content.generated.heroSubheadline}
        </p>
      </div>

      {content.facts.rating && (
        <div className="px-8 py-4 text-center border-b border-slate-100 text-sm text-slate-600">
          ⭐ {content.facts.rating} no Google{content.facts.userRatingCount ? ` · ${content.facts.userRatingCount} avaliações` : ""}
        </div>
      )}

      {content.photos.photoUrls.length > 0 && (
        <div className="grid grid-cols-3 gap-1 p-1">
          {content.photos.photoUrls.map((url) => (
            // eslint-disable-next-line @next/next/no-img-element
            <img key={url} src={url} alt={content.facts.name} className="w-full h-32 object-cover" />
          ))}
        </div>
      )}

      <div className="px-8 py-10">
        <h2 className="text-xl font-bold mb-3" style={accentColor ? { color: accentColor } : undefined}>
          Sobre
        </h2>
        <p className="text-slate-600 leading-relaxed">{content.generated.aboutCopy}</p>
      </div>

      <div className="px-8 py-10 bg-slate-50">
        <h2 className="text-xl font-bold mb-4" style={accentColor ? { color: accentColor } : undefined}>
          Serviços
        </h2>
        <div className="grid gap-4 sm:grid-cols-2">
          {content.generated.services.map((service) => (
            <div key={service.title} className="bg-white rounded-xl p-4 border border-slate-100">
              <p className="font-bold mb-1">{service.title}</p>
              <p className="text-sm text-slate-600">{service.description}</p>
            </div>
          ))}
        </div>
      </div>

      {content.facts.address && (
        <div className="px-8 py-6 text-sm text-slate-500 border-t border-slate-100">{content.facts.address}</div>
      )}
    </div>
  );
}
```
  </action>
  <verify>grep -q "theme?.primaryColor" "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx" && npm run build</verify>
  <done>Preview usa a cor extraída como destaque quando presente; fallback neutro preservado; build compila sem erro de tipo.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `types/redesign-content.ts`: `theme` existe como chave-irmã opcional/nullable, não aninhada em `photos`
- [ ] `lib/screenshot.ts`: `captureSiteVisuals` existe, nenhuma referência a `captureScreenshot` sobra no projeto (`grep -r captureScreenshot app/ lib/` vazio)
- [ ] `lib/site-scrape.ts`: `scrapeSiteAssets` existe, nunca lança (try/catch envolve todo o corpo)
- [ ] `app/api/redesigns/generate/route.ts`: chamadas a `captureSiteVisuals`/`scrapeSiteAssets`/`uploadFromUrl` ficam fisicamente depois do bloco `catch` que devolve 502
- [ ] `app/api/redesigns/[id]/route.ts`: PATCH inclui `theme: currentContent.theme` e o tipo do `body` desestruturado não tem campo `theme`
- [ ] `app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx`: usa `content.theme?.primaryColor` com fallback pro template neutro
- [ ] `app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx`: NÃO foi modificado (confirmado por research: payload de save continua só `generated`+`photos`)
- [ ] Nenhuma migration SQL nova (confirmado: `redesigns.content` é jsonb sem CHECK constraint, campo aditivo e opcional)
</verification>

<success_criteria>
- Todas as 6 tasks completas
- Build limpo (`npm run build`)
- Um redesign gerado a partir de agora, pra um lead com site próprio que tenha logo/fotos/theme-color extraíveis, produz `content.theme.primaryColor` não-nulo e `content.photos.logoUrl`/`photoUrls` vindos do site original (verificável manualmente após deploy, já que geração real exige `AI_GATEWAY_API_KEY`/produção)
- Um lead sem site próprio (ou cujo site bloqueia scraping/não declara nada extraível) produz exatamente o redesign de hoje: `theme: null`, `photos.logoUrl: null`, `photos.photoUrls` = fotos do Places
- Redesigns já existentes no banco continuam renderizando normalmente (sem `theme` na linha = `undefined`, tratado como ausente)
</success_criteria>

<output>
Após completar, criar `.planning/phases/02.1-reusar-logo-paleta-de-cores-e-fotos-do-site-original-do-lead-no-redesign-gerado/02.1-01-SUMMARY.md`
</output>
