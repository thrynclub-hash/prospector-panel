---
phase: 05-proposta
plan: 05
type: execute
wave: 2
depends_on: ["05-02"]
files_modified: ["lib/email/proposal.ts", "app/api/redesigns/[id]/proposal/send/route.ts", "app/unsubscribe/[token]/page.tsx"]
autonomous: true
requirements: [PROPOSTA-03, PROPOSTA-04]
user_setup: []

must_haves:
  truths:
    - "Enviar por e-mail passa por uma checagem de contacted_businesses (por place_id) ANTES de chamar o Resend -- um negócio já contatado (por qualquer assinante) ou que pediu opt-out nunca recebe de novo"
    - "Um envio bem-sucedido grava contacted_businesses (place_id + opt_out_token) DEPOIS do Resend confirmar sucesso, nunca antes"
    - "Existe uma rota pública /unsubscribe/[token] que marca opted_out_at sem exigir login, sem usar o client admin -- só lib/supabase/public.ts chamando a função security definer opt_out_business(token) via .rpc(), mesmo isolamento da Fase 4 (a comparação do token acontece dentro da função no banco, não numa policy RLS nem num filtro .eq() do cliente)"
    - "O e-mail de proposta usa uma identidade de remetente distinta do magic-link e inclui o header List-Unsubscribe + link visível no corpo"
  artifacts:
    - path: "lib/email/proposal.ts"
      provides: "sendProposalEmail() com remetente propostas@ + List-Unsubscribe"
      contains: "List-Unsubscribe"
    - path: "app/api/redesigns/[id]/proposal/send/route.ts"
      provides: "POST que checa supressão, envia, e só então marca contacted_businesses"
      contains: "contacted_businesses"
    - path: "app/unsubscribe/[token]/page.tsx"
      provides: "Página pública de opt-out, sem sessão, sem admin client"
      contains: "createSupabasePublicClient"
  key_links:
    - from: "app/api/redesigns/[id]/proposal/send/route.ts"
      to: "lib/email/proposal.ts"
      via: "chama sendProposalEmail() só depois de passar a checagem de supressão"
      pattern: "sendProposalEmail"
    - from: "app/unsubscribe/[token]/page.tsx"
      to: "lib/supabase/public.ts"
      via: "createSupabasePublicClient() -- nunca o client de sessão nem o admin"
      pattern: "createSupabasePublicClient"
---

<objective>
PITFALLS.md Pitfall 6 (05-RESEARCH.md) é explícito: suprimir e permitir opt-out são estruturais a esta fase, não um follow-up -- "a suppression list e o opt-out mechanism... devem ser desenhados dentro dessa fase, não emendados depois que reclamações chegarem". Este plan cobre PROPOSTA-03 (envio real via Resend) e PROPOSTA-04 (supressão + opt-out) juntos porque são a mesma peça de risco: nenhuma delas deve existir sem a outra.

Purpose: o botão de "enviar e-mail" da Plan 05-06 só pode existir depois desta rota existir de verdade -- e a rota de opt-out precisa estar no ar ANTES do primeiro e-mail real sair, já que o link de opt-out vai dentro do e-mail.
Output: lib de envio distinta da de magic-link, rota de envio com checagem de supressão, e a página pública de opt-out.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proposta/05-CONTEXT.md
@.planning/phases/05-proposta/05-RESEARCH.md
@lib/email/resend.ts
@lib/supabase/public.ts
@app/demo/[slug]/page.tsx
@app/api/redesigns/[id]/publish/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/email/proposal.ts -- envio distinto do magic-link</name>
  <files>lib/email/proposal.ts</files>
  <action>
Criar `lib/email/proposal.ts` (NUNCA estender `sendMagicLinkEmail` -- 05-RESEARCH.md Pitfall 4: reusar a mesma identidade de remetente e pular o header de unsubscribe mistura transacional com outreach frio):

```ts
import { Resend } from "resend";

// 05-RESEARCH.md Pitfall 4 / Architecture Patterns §7: NUNCA reusar
// sendMagicLinkEmail pra e-mail de outreach frio -- identidade de remetente
// distinta ("propostas@" em vez de "acesso@") e List-Unsubscribe
// obrigatório, já que este e-mail não é transacional. Client
// function-scoped (não singleton de módulo) -- mesma regra de
// lib/email/resend.ts (ARCHITECTURE.md §0: Resend já derrubou um build por
// isso).
export async function sendProposalEmail({
  to,
  subject,
  body,
  unsubscribeUrl,
}: {
  to: string;
  subject: string;
  body: string;
  unsubscribeUrl: string;
}) {
  const resend = new Resend(process.env.RESEND_API_KEY);
  await resend.emails.send({
    from: "Hunter of Bad Pages <propostas@toqy.com.br>",
    to,
    subject,
    html: `
      <div style="font-family: sans-serif; max-width: 480px; margin: 0 auto; padding: 32px; white-space: pre-wrap; color: #17141a; line-height: 1.6;">
        ${body}
        <p style="color: #6b6560; font-size: 12px; margin-top: 32px; border-top: 1px solid #e5e0da; padding-top: 16px;">
          Não quer mais receber contatos como este?
          <a href="${unsubscribeUrl}" style="color: #6b6560;">Clique aqui</a>.
        </p>
      </div>
    `,
    headers: {
      "List-Unsubscribe": `<${unsubscribeUrl}>`,
      "List-Unsubscribe-Post": "List-Unsubscribe=One-Click",
    },
  });
}
```

Remetente `propostas@toqy.com.br` -- mesmo domínio já verificado no Resend (`lib/email/resend.ts` usa `acesso@toqy.com.br`), local-part diferente pra separar identidade transacional de outreach.
  </action>
  <verify>grep -q "List-Unsubscribe" lib/email/proposal.ts && grep -q "propostas@" lib/email/proposal.ts</verify>
  <done>Função criada, remetente distinto, header + link de unsubscribe presentes, client function-scoped (não módulo).</done>
</task>

<task type="auto">
  <name>Task 2: Rota de envio -- checa supressão, envia, só então marca contacted_businesses</name>
  <files>app/api/redesigns/[id]/proposal/send/route.ts</files>
  <action>
Criar `app/api/redesigns/[id]/proposal/send/route.ts`:

```ts
import { NextResponse } from "next/server";
import { nanoid } from "nanoid";
import { requireActiveUser } from "@/lib/auth-guard";
import { sendProposalEmail } from "@/lib/email/proposal";

// PROPOSTA-03/04: envio de e-mail automático só acontece depois de checar a
// lista de supressão (contacted_businesses) -- e só DEPOIS de um envio
// efetivamente bem-sucedido é que o place_id entra na lista (CONTEXT.md:
// "não precisa de resposta ou reclamação pra isso").
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) {
  const guard = await requireActiveUser();
  if ("error" in guard) return guard.error;
  const { supabase, user } = guard;

  const { id } = await params;

  const { data: redesign, error: redesignError } = await supabase
    .from("redesigns")
    .select("id, lead_id, is_public")
    .eq("id", id)
    .eq("user_id", user.id)
    .maybeSingle();

  if (redesignError || !redesign) {
    return NextResponse.json({ error: "Redesign não encontrado" }, { status: 404 });
  }

  if (!redesign.is_public) {
    return NextResponse.json({ error: "Publique o redesign antes de enviar a proposta" }, { status: 409 });
  }

  const { data: proposal } = await supabase
    .from("proposals")
    .select("id, email_subject, email_body")
    .eq("redesign_id", id)
    .eq("user_id", user.id)
    .maybeSingle();

  if (!proposal) {
    return NextResponse.json({ error: "Gere a proposta antes de enviar" }, { status: 404 });
  }

  const { data: lead, error: leadError } = await supabase
    .from("leads")
    .select("place_id, public_email")
    .eq("id", redesign.lead_id)
    .eq("user_id", user.id)
    .maybeSingle();

  if (leadError || !lead) {
    return NextResponse.json({ error: "Lead não encontrado" }, { status: 404 });
  }

  if (!lead.public_email) {
    return NextResponse.json({ error: "Este lead não tem e-mail público" }, { status: 400 });
  }

  const { data: suppression } = await supabase
    .from("contacted_businesses")
    .select("opted_out_at")
    .eq("place_id", lead.place_id)
    .maybeSingle();

  if (suppression) {
    const message = suppression.opted_out_at
      ? "Este negócio pediu pra não receber contatos automáticos"
      : "Este negócio já foi contatado por e-mail (por você ou outro assinante)";
    return NextResponse.json({ error: message }, { status: 409 });
  }

  const token = nanoid(32);
  const unsubscribeUrl = `${process.env.NEXT_PUBLIC_APP_URL}/unsubscribe/${token}`;

  try {
    await sendProposalEmail({
      to: lead.public_email,
      subject: proposal.email_subject,
      body: proposal.email_body,
      unsubscribeUrl,
    });
  } catch (err) {
    const message = err instanceof Error ? err.message : String(err);
    console.error("redesigns/proposal/send: erro enviando e-mail", err);
    return NextResponse.json({ error: `Envio falhou: ${message}` }, { status: 502 });
  }

  // Só entra na lista de supressão DEPOIS do envio ter sucesso de fato. Uma
  // corrida entre dois assinantes enviando pro mesmo place_id ao mesmo tempo
  // é uma janela teórica aceita nesta escala de produto -- o pre-check acima
  // cobre o caso comum (sequencial), e um erro aqui não desfaz o e-mail já
  // enviado, só falha silenciosamente em registrar a supressão (logado).
  const { error: suppressionError } = await supabase.from("contacted_businesses").insert({
    place_id: lead.place_id,
    contacted_by_user_id: user.id,
    contacted_via: "email",
    opt_out_token: token,
  });
  if (suppressionError) {
    console.error("redesigns/proposal/send: erro registrando supressão", suppressionError);
  }

  await supabase.from("proposals").update({ email_sent_at: new Date().toISOString() }).eq("id", proposal.id);

  return NextResponse.json({ ok: true });
}
```
  </action>
  <verify>grep -q "contacted_businesses" "app/api/redesigns/[id]/proposal/send/route.ts" && grep -q "sendProposalEmail" "app/api/redesigns/[id]/proposal/send/route.ts"</verify>
  <done>Rota checa supressão antes de enviar, envia, e só grava contacted_businesses/email_sent_at após sucesso confirmado do Resend.</done>
</task>

<task type="auto">
  <name>Task 3: Página pública de opt-out</name>
  <files>app/unsubscribe/[token]/page.tsx</files>
  <action>
Criar `app/unsubscribe/[token]/page.tsx` (Server Component, sem "use client" -- mesmo padrão de `app/demo/[slug]/page.tsx`; `route.ts` não pode coexistir com `page.tsx` no mesmo segmento no App Router, por isso é página, não Route Handler):

```tsx
import { CheckCircle2 } from "lucide-react";
import { createSupabasePublicClient } from "@/lib/supabase/public";

// Único WRITE público deste projeto (Fase 4 só tinha leitura pública). O
// token é a autorização, mas a comparação de fato acontece DENTRO da função
// `opt_out_business` (security definer, definida na migration 05-02) --
// nunca numa policy RLS nem num filtro `.eq()` deste cliente. Uma policy RLS
// do tipo `using (opt_out_token is not null)` pareceria certa mas não
// compararia o token recebido contra o token real da linha (toda linha tem
// token preenchido desde o insert); só o corpo da função tem acesso ao
// parâmetro `token` pra fazer essa comparação. Por isso não existe select
// nem update direto pro papel anon nesta tabela -- só `execute` na função.
export default async function UnsubscribePage({ params }: { params: Promise<{ token: string }> }) {
  const { token } = await params;
  const supabase = createSupabasePublicClient();

  const { data, error } = await supabase.rpc("opt_out_business", { token });

  // Resultado vazio (nenhuma linha casou o token) = token inválido/inexistente.
  const row = data?.[0];
  const success = !error && !!row;
  const alreadyOptedOut = row?.already_opted_out ?? false;

  return (
    <main className="min-h-screen bg-bg flex items-center justify-center px-6">
      <div className="max-w-sm text-center bg-card border border-border rounded-2xl p-8">
        {error || !success ? (
          <p className="text-bad">
            {error
              ? "Não foi possível processar seu pedido agora. Tente de novo mais tarde."
              : "Este link não é válido."}
          </p>
        ) : (
          <>
            <CheckCircle2 className="mx-auto mb-4 text-good" size={40} />
            <h1 className="font-display font-bold text-xl text-ink mb-2">
              {alreadyOptedOut ? "Pedido já registrado" : "Você não receberá mais contatos como este"}
            </h1>
            <p className="text-sm text-muted">
              {alreadyOptedOut
                ? "Este link já foi usado antes -- este contato já estava fora da nossa lista de envio automático."
                : "Removemos este contato da nossa lista de envio automático."}
            </p>
          </>
        )}
      </div>
    </main>
  );
}
```

Idempotência agora vem de dentro da função (`coalesce(cb.opted_out_at, now())` na migration 05-02): clicar o link duas vezes não reseta o timestamp nem gera erro, e o campo `already_opted_out` retornado pela função permite distinguir "primeira vez" de "já tinha pedido antes" -- exatamente como CONTEXT.md pede. Token inexistente (nenhuma linha casa) retorna array vazio, tratado aqui como link inválido.
  </action>
  <verify>grep -q "createSupabasePublicClient" "app/unsubscribe/[token]/page.tsx" && npm run build</verify>
  <done>Página pública criada, sem sessão nem admin client, idempotente, build compila.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] Rota de envio retorna 409 se `contacted_businesses` já tem uma linha pra esse `place_id` (contatado OU opt-out), com mensagens distintas
- [ ] Rota de envio só insere em `contacted_businesses` DEPOIS de `sendProposalEmail` não lançar
- [ ] `lib/email/proposal.ts` usa remetente diferente de `lib/email/resend.ts` e inclui `List-Unsubscribe`
- [ ] `app/unsubscribe/[token]/page.tsx` nunca importa `lib/supabase/server.ts` nem `lib/supabase/admin.ts`, e nunca faz `.from("contacted_businesses").update(...)` direto -- só `.rpc("opt_out_business", { token })`
- [ ] Clicar o link de opt-out duas vezes não gera erro (idempotente via `coalesce` dentro da função `opt_out_business`, não via filtro do cliente); token inexistente/inválido é tratado como link inválido, não como sucesso
</verification>

<success_criteria>
- Todas as tasks completas
- Build limpo
- Envio real (produção, `RESEND_API_KEY` já configurada) bloqueado corretamente por supressão e testável manualmente após deploy
</success_criteria>

<output>
Após completar, criar `.planning/phases/05-proposta/05-05-SUMMARY.md`
</output>
