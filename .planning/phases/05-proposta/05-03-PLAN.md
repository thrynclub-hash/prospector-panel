---
phase: 05-proposta
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [lib/proposal/detect-problems.ts, lib/ai/generate-proposal.ts]
autonomous: true
requirements: [PROPOSTA-01]
user_setup: []

must_haves:
  truths:
    - "detectSiteProblems() deriva problemas específicos e verdadeiros a partir de leads.has_own_website/pagespeed_score -- nunca produz a mesma frase genérica pra leads com sinais diferentes (ex.: PageSpeed 12 vs PageSpeed 48 geram textos distintos)"
    - "generateProposalCopy() produz os dois formatos (e-mail + WhatsApp) numa única chamada generateObject, seguindo as regras invioláveis: sem preço, sem checklist fixo genérico, tom de oportunidade construtiva"
  artifacts:
    - path: "lib/proposal/detect-problems.ts"
      provides: "Função pura detectSiteProblems(lead): string[]"
      contains: "export function detectSiteProblems"
    - path: "lib/ai/generate-proposal.ts"
      provides: "generateProposalCopy() via generateObject, schema Zod com emailSubject/emailBody/whatsappText"
      contains: "export async function generateProposalCopy"
  key_links:
    - from: "lib/ai/generate-proposal.ts"
      to: "lib/proposal/detect-problems.ts"
      via: "consumidor esperado (rota da Plan 05-04) passa problems: string[] de detectSiteProblems() como input de generateProposalCopy()"
      pattern: "problems: string\\[\\]"
---

<objective>
PROPOSTA-01 exige "problemas específicos detectados naquele site" -- 05-RESEARCH.md (Pitfall 6) alerta que reusar o `badSiteReason` de `generate-redesign.ts` (duas strings fixas: "Sem site próprio" / "Site próprio com problemas de qualidade/performance") produziria o mesmo texto genérico pra leads com sinais bem diferentes, violando CONTEXT.md ("nunca um checklist fixo genérico"). A correção é uma função DETERMINÍSTICA (não mais uma chamada de IA) que lê os dados reais já existentes em `leads` (`has_own_website`, `pagespeed_score`), alimentando uma única chamada `generateObject` que produz os dois formatos de proposta de uma vez (locked decision de CONTEXT.md).

Purpose: separar detecção de fato (determinística) de geração de copy (IA), mesmo padrão já estabelecido por REDESENHAR-02/`generate-redesign.ts`.
Output: duas libs novas, sem dependência de banco ou rota -- puramente funções, prontas pra Plan 05-04 consumir.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proposta/05-CONTEXT.md
@.planning/phases/05-proposta/05-RESEARCH.md
@lib/ai/generate-redesign.ts
@app/api/leads/search/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/proposal/detect-problems.ts</name>
  <files>lib/proposal/detect-problems.ts</files>
  <action>
Criar `lib/proposal/detect-problems.ts` (novo diretório `lib/proposal/`) com:

```ts
// PROPOSTA-01 / CONTEXT.md: "citar só os problemas que se aplicam a esse
// lead específico... nunca um checklist fixo genérico" -- 05-RESEARCH.md
// Pitfall 6 alerta que reusar o badSiteReason de generate-redesign.ts (duas
// strings fixas) produziria o mesmo texto pra leads com sinais bem
// diferentes (PageSpeed 12 vs 48). Função pura, não uma chamada de IA --
// mesmo racional de generate-redesign.ts: só o que é verificável entra no
// prompt de geração.
export interface LeadForProblemDetection {
  hasOwnWebsite: boolean;
  pagespeedScore: number | null;
}

export function detectSiteProblems(lead: LeadForProblemDetection): string[] {
  if (!lead.hasOwnWebsite) {
    return ["o negócio ainda não tem um site próprio"];
  }

  const problems: string[] = [];
  if (lead.pagespeedScore !== null && lead.pagespeedScore < 50) {
    problems.push(`o site atual tem desempenho baixo no Google PageSpeed (${lead.pagespeedScore}/100 no mobile)`);
  }

  return problems.length > 0 ? problems : ["o site atual pode ser modernizado visualmente"];
}
```
  </action>
  <verify>grep -q "export function detectSiteProblems" lib/proposal/detect-problems.ts</verify>
  <done>Função pura criada, sem chamadas de rede/IA, cobre os 3 casos (sem site, PageSpeed baixo, site ok mas datado).</done>
</task>

<task type="auto">
  <name>Task 2: lib/ai/generate-proposal.ts</name>
  <files>lib/ai/generate-proposal.ts</files>
  <action>
Criar `lib/ai/generate-proposal.ts`, seguindo exatamente o padrão de `lib/ai/generate-redesign.ts` (mesmo model tier, mesma estrutura "REGRAS INVIOLÁVEIS"):

```ts
import { generateObject } from "ai";
import { z } from "zod";

// Modelo via Vercel AI Gateway -- mesma string/tier de generate-redesign.ts.
// Exige AI_GATEWAY_API_KEY em dev local (produção usa OIDC automático).
const MODEL = "google/gemini-2.5-flash";

const ProposalSchema = z.object({
  emailSubject: z.string().describe("Assunto específico ao negócio, curto, sem cara de marketing/spam"),
  emailBody: z.string().describe("Corpo do e-mail, 120-180 palavras, sem preço, tom de oportunidade construtiva"),
  whatsappText: z.string().describe("Versão curta e mais informal do mesmo conteúdo-base, adequada a uma mensagem de WhatsApp"),
});

export interface ProposalCopyInput {
  name: string;
  address: string | null;
  rating: number | null;
  userRatingCount: number | null;
  problems: string[];
  demoUrl: string;
}

// PROPOSTA-01: mesmo mecanismo de "REGRAS INVIOLÁVEIS" de generate-redesign.ts
// -- sem preço, sem checklist genérico, tom de oportunidade (nunca "seu site
// está ruim"). Estrutura segue a skill proposta-email (rapport real >
// defeito com delicadeza > entrega > CTA leve), já resumida em CONTEXT.md.
export async function generateProposalCopy(input: ProposalCopyInput) {
  const { object } = await generateObject({
    model: MODEL,
    schema: ProposalSchema,
    prompt: `Você está escrevendo uma proposta comercial fria para o dono de um negócio real, a partir SÓ dos dados abaixo.

DADOS REAIS (única fonte permitida):
- Nome do negócio: ${input.name}
- Endereço: ${input.address ?? "não informado"}
- Nota no Google: ${input.rating ?? "não informada"}${input.userRatingCount ? ` (${input.userRatingCount} avaliações)` : ""}
- Problema(s) identificado(s) neste site específico: ${input.problems.join("; ")}
- Link da versão nova já pronta e publicada: ${input.demoUrl}

ESTRUTURA OBRIGATÓRIA (skill proposta-email):
1. Rapport real: abra citando a nota/quantidade de avaliações do Google (só se disponíveis) -- elogio específico e verificável, nunca genérico.
2. Problema com delicadeza: mencione APENAS os problemas listados acima, enquadrados como oportunidade de melhoria ("percebi que..."), nunca como defeito ("seu site está ruim").
3. Entrega como argumento: "preparei uma nova versão, já no ar" + o link. A entrega já pronta é o argumento, não um orçamento abstrato.
4. CTA leve: peça só que abram o link e digam o que acharam. Sem urgência artificial, sem "vagas limitadas".

REGRAS INVIOLÁVEIS:
1. NUNCA mencione preço, orçamento ou valor -- em nenhuma das duas versões.
2. NUNCA use um checklist fixo de problemas -- cite só os problemas informados acima, com as palavras deles.
3. NUNCA chame o site atual de "ruim", "péssimo" ou similar -- tom de oportunidade construtiva.
4. emailBody: 120-180 palavras. whatsappText: mais curto e informal, mesmo conteúdo-base, sem formatação de e-mail (sem "Prezado(a)", sem assinatura formal).
5. emailSubject: específico ao negócio, sem cara de marketing/spam.`,
  });

  return object;
}
```
  </action>
  <verify>grep -q "export async function generateProposalCopy" lib/ai/generate-proposal.ts && npm run build</verify>
  <done>Função criada, mesma forma de generate-redesign.ts, schema Zod com os 3 campos, build compila.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `detectSiteProblems({ hasOwnWebsite: false, pagespeedScore: null })` retorna a frase de "sem site"
- [ ] `detectSiteProblems({ hasOwnWebsite: true, pagespeedScore: 30 })` retorna frase citando o score 30, diferente de `pagespeedScore: 90`
- [ ] `generateProposalCopy()` nunca recebe `badSiteReason` como input -- só `problems: string[]` vindo de `detectSiteProblems`
- [ ] Prompt não permite preço/checklist fixo/tom negativo (regras explícitas no texto)
</verification>

<success_criteria>
- Ambas as libs criadas, sem dependência de Supabase/rotas
- Build limpo
- Prontas pra Plan 05-04 importar e consumir
</success_criteria>

<output>
Após completar, criar `.planning/phases/05-proposta/05-03-SUMMARY.md`
</output>
