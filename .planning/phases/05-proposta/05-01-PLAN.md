---
phase: 05-proposta
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/google-places/client.ts, app/api/redesigns/generate/route.ts]
autonomous: true
requirements: [PROPOSTA-02]
user_setup: []

must_haves:
  truths:
    - "Novos redesigns gerados a partir de agora têm content.facts.phone preenchido com o telefone internacional do Places quando o negócio tiver esse dado público, em vez de sempre null"
    - "O field mask do Places (Text Search e Place Details) passa a pedir o campo de telefone, e o comentário sobre tier de billing no arquivo reflete a verificação feita nesta tarefa"
  artifacts:
    - path: "lib/google-places/client.ts"
      provides: "PlaceResult.internationalPhoneNumber incluído no field mask de Details e Text Search"
      contains: "internationalPhoneNumber"
    - path: "app/api/redesigns/generate/route.ts"
      provides: "facts.phone populado a partir de details.internationalPhoneNumber em vez de null fixo"
      contains: "details.internationalPhoneNumber"
  key_links:
    - from: "app/api/redesigns/generate/route.ts"
      to: "lib/google-places/client.ts"
      via: "details retornado por placesClient.getDetails() agora inclui internationalPhoneNumber, lido em facts.phone"
      pattern: "phone: details\\.internationalPhoneNumber"
---

<objective>
05-RESEARCH.md (Pitfall 1) identificou que `redesigns.content.facts.phone` é hoje SEMPRE `null` -- `app/api/redesigns/generate/route.ts` hardcoda `phone: null`, e nenhum field mask do Places pede um campo de telefone. CONTEXT.md (locked decision) assume que esse campo "já existe desde a Fase 2" pro botão de WhatsApp (PROPOSTA-02) -- não existe de verdade. Esta é a Plan 1 da Fase 5 porque bloqueia a UI da Plan 05-06 (botão de WhatsApp só aparece quando `facts.phone` não é null).

Purpose: fechar esse gap ANTES de qualquer trabalho de UI depender dele.
Output: field masks do Places pedindo telefone + `generate/route.ts` populando `facts.phone` de verdade.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proposta/05-CONTEXT.md
@.planning/phases/05-proposta/05-RESEARCH.md
@lib/google-places/client.ts
@app/api/redesigns/generate/route.ts
@types/redesign-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo de telefone aos field masks do Places + interface</name>
  <files>lib/google-places/client.ts</files>
  <action>
Em `lib/google-places/client.ts`:

1. Adicionar `"places.internationalPhoneNumber"` ao array `PRO_TIER_FIELD_MASK` (junto dos outros `places.*`).
2. Adicionar `"internationalPhoneNumber"` ao array `DETAILS_FIELD_MASK`.
3. Adicionar `internationalPhoneNumber?: string;` à interface `PlaceResult` (junto de `websiteUri`, etc).
4. Atualizar o comentário no topo do arquivo (que hoje diz "Field mask restrito aos campos do tier Pro ($32/1000)") acrescentando uma nota:

```
// NOTA (2026-07, Fase 5): segundo a documentação atual do Google (Place Data
// Fields), `internationalPhoneNumber` é campo tier Enterprise -- mas
// `rating`, `userRatingCount` e `websiteUri` (já pedidos acima) TAMBÉM são
// Enterprise hoje, então esta chamada provavelmente já era billada nesse
// tier antes desta mudança, independente dela. O comentário "Pro tier" acima
// pode estar desatualizado -- reverificar em
// https://developers.google.com/maps/documentation/places/web-service/data-fields
// se o custo real da fatura do Google Cloud for motivo de dúvida.
```

Não remover nenhum campo existente dos field masks -- só adicionar.
  </action>
  <verify>grep -q "internationalPhoneNumber" lib/google-places/client.ts</verify>
  <done>Field masks (Text Search e Details) incluem internationalPhoneNumber, PlaceResult tem o campo tipado, comentário de billing atualizado.</done>
</task>

<task type="auto">
  <name>Task 2: Popular content.facts.phone na rota de geração</name>
  <files>app/api/redesigns/generate/route.ts</files>
  <action>
Em `app/api/redesigns/generate/route.ts`, dentro do objeto `content.facts` (hoje na linha ~87 com `phone: null,`), trocar por:

```ts
phone: details.internationalPhoneNumber ?? null,
```

Nada mais muda nessa rota. Redesigns já existentes (gerados antes desta mudança) continuam com `phone: null` pra sempre -- não há backfill nesta tarefa, e não é necessário: a regra "esconder o botão se o dado faltar" (já travada em CONTEXT.md) cobre isso graciosamente pra linhas antigas.
  </action>
  <verify>grep -q "details.internationalPhoneNumber" app/api/redesigns/generate/route.ts && npm run build</verify>
  <done>facts.phone é atribuído dinamicamente a partir do Places; build compila sem erro de tipo.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `lib/google-places/client.ts`: `PRO_TIER_FIELD_MASK`, `DETAILS_FIELD_MASK` e `PlaceResult` incluem o campo de telefone
- [ ] `app/api/redesigns/generate/route.ts` não tem mais `phone: null,` hardcoded
- [ ] Comentário de billing atualizado com a nota sobre tier Enterprise
</verification>

<success_criteria>
- Todas as tasks completas
- Build limpo
- Um redesign gerado a partir de agora, pra um lead com telefone público no Google, produz `content.facts.phone` não-nulo (verificável manualmente após deploy, já que geração real exige `AI_GATEWAY_API_KEY`/produção)
</success_criteria>

<output>
Após completar, criar `.planning/phases/05-proposta/05-01-SUMMARY.md`
</output>
