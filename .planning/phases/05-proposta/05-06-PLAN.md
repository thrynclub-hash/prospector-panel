---
phase: 05-proposta
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-04", "05-05"]
files_modified: ["lib/proposal/whatsapp-link.ts", "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx", "app/painel/leads/[leadId]/redesenhar/page.tsx"]
autonomous: true
requirements: [PROPOSTA-01, PROPOSTA-02, PROPOSTA-03, PROPOSTA-04]
user_setup: []

must_haves:
  truths:
    - "A seção de proposta vive dentro de /painel/leads/[leadId]/redesenhar, sem rota separada, e só aparece de fato quando o redesign existe"
    - "Botão de gerar proposta fica desabilitado/ausente com aviso 'publique primeiro' enquanto is_public=false"
    - "Botão de WhatsApp só aparece quando content.facts.phone existe; botão de e-mail só aparece quando lead.public_email existe -- nunca desabilitado por falta de dado, apenas escondido"
    - "Quando o lead está suprimido (contacted_businesses tem linha pro place_id), o botão de e-mail aparece DESABILITADO com aviso explicativo -- WhatsApp continua liberado"
    - "Envio de e-mail exige confirmação explícita em dois cliques (mesmo padrão de DeleteRedesignButton), nunca automático ao gerar"
  artifacts:
    - path: "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx"
      provides: "Componente client com geração, edição (textarea), WhatsApp link e envio de e-mail com confirmação"
      contains: "ProposalSection"
    - path: "app/painel/leads/[leadId]/redesenhar/page.tsx"
      provides: "Query de public_email + suppression + proposal, renderização condicional de ProposalSection"
      contains: "contacted_businesses"
  key_links:
    - from: "app/painel/leads/[leadId]/redesenhar/page.tsx"
      to: "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx"
      via: "renderiza <ProposalSection> passando phone/publicEmail/suppression/initialProposal quando redesign existe"
      pattern: "ProposalSection"
    - from: "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx"
      to: "app/api/redesigns/[id]/proposal/route.ts"
      via: "fetch POST (gerar) e PATCH (editar) pra /api/redesigns/{id}/proposal"
      pattern: "/proposal"
    - from: "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx"
      to: "app/api/redesigns/[id]/proposal/send/route.ts"
      via: "fetch POST pra /api/redesigns/{id}/proposal/send só após confirmação em dois cliques"
      pattern: "/proposal/send"
---

<objective>
Última wave da Fase 5: expõe tudo que as Plans 05-01/05-04/05-05 construíram (phone populado, rota de geração/edição, rota de envio + opt-out) na tela onde CONTEXT.md manda que a proposta viva -- dentro de `/painel/leads/[leadId]/redesenhar`, junto de Editor/Publicar, sem navegação nova.

Purpose: fechar as 4 requirements da fase (PROPOSTA-01 a 04) numa interface usável, respeitando as regras de "esconder vs desabilitar" e a confirmação explícita antes de qualquer envio automático.
Output: um componente novo (`proposal-section.tsx`), uma lib pequena (`whatsapp-link.ts`), e `page.tsx` atualizado pra buscar os dados que faltam (`public_email`, supressão, proposta existente) e renderizar o componente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-proposta/05-CONTEXT.md
@.planning/phases/05-proposta/05-RESEARCH.md
@app/painel/leads/[leadId]/redesenhar/page.tsx
@app/painel/leads/[leadId]/redesenhar/publish-button.tsx
@app/painel/leads/[leadId]/redesenhar/delete-redesign-button.tsx
@app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx
@types/redesign-content.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/proposal/whatsapp-link.ts</name>
  <files>lib/proposal/whatsapp-link.ts</files>
  <action>
Criar `lib/proposal/whatsapp-link.ts`:

```ts
// PROPOSTA-02: wa.me precisa do número só com dígitos (sem "+", espaços ou
// hífens) -- o dado vem de content.facts.phone (internationalPhoneNumber do
// Places, ex. "+55 11 99999-9999", populado desde a Plan 05-01).
export function toWhatsAppLink(internationalPhone: string, text: string): string {
  const digitsOnly = internationalPhone.replace(/\D/g, "");
  return `https://wa.me/${digitsOnly}?text=${encodeURIComponent(text)}`;
}
```
  </action>
  <verify>grep -q "export function toWhatsAppLink" lib/proposal/whatsapp-link.ts</verify>
  <done>Função pura criada, sem dependência de DOM/rede.</done>
</task>

<task type="auto">
  <name>Task 2: proposal-section.tsx -- componente client completo</name>
  <files>app/painel/leads/[leadId]/redesenhar/proposal-section.tsx</files>
  <action>
Criar `app/painel/leads/[leadId]/redesenhar/proposal-section.tsx`, seguindo o padrão de estado local (`useState`, sem lib externa) já usado por `editor-form.tsx`/`publish-button.tsx`, e o padrão de confirmação em dois cliques de `delete-redesign-button.tsx` pro botão de envio de e-mail:

```tsx
"use client";

import { useState } from "react";
import { MessageCircle, Mail, Loader2, Sparkles, Save } from "lucide-react";
import { toWhatsAppLink } from "@/lib/proposal/whatsapp-link";

interface Proposal {
  id: string;
  email_subject: string;
  email_body: string;
  whatsapp_text: string;
  email_sent_at: string | null;
}

interface Suppression {
  opted_out_at: string | null;
}

export function ProposalSection({
  redesignId,
  isPublic,
  phone,
  publicEmail,
  initialProposal,
  suppression,
}: {
  redesignId: string;
  isPublic: boolean;
  phone: string | null;
  publicEmail: string | null;
  initialProposal: Proposal | null;
  suppression: Suppression | null;
}) {
  const [proposal, setProposal] = useState(initialProposal);
  const [emailSubject, setEmailSubject] = useState(initialProposal?.email_subject ?? "");
  const [emailBody, setEmailBody] = useState(initialProposal?.email_body ?? "");
  const [whatsappText, setWhatsappText] = useState(initialProposal?.whatsapp_text ?? "");
  const [generating, setGenerating] = useState(false);
  const [saving, setSaving] = useState(false);
  const [confirmingSend, setConfirmingSend] = useState(false);
  const [sending, setSending] = useState(false);
  const [error, setError] = useState<string | null>(null);

  async function handleGenerate() {
    setGenerating(true);
    setError(null);
    try {
      const res = await fetch(`/api/redesigns/${redesignId}/proposal`, { method: "POST" });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? "Geração falhou");
        return;
      }
      setProposal(data.proposal);
      setEmailSubject(data.proposal.email_subject);
      setEmailBody(data.proposal.email_body);
      setWhatsappText(data.proposal.whatsapp_text);
    } catch {
      setError("Erro de conexão. Tenta de novo.");
    } finally {
      setGenerating(false);
    }
  }

  async function saveEdits(): Promise<boolean> {
    setSaving(true);
    setError(null);
    try {
      const res = await fetch(`/api/redesigns/${redesignId}/proposal`, {
        method: "PATCH",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ emailSubject, emailBody, whatsappText }),
      });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? "Erro salvando");
        return false;
      }
      setProposal(data.proposal);
      return true;
    } catch {
      setError("Erro de conexão. Tenta de novo.");
      return false;
    } finally {
      setSaving(false);
    }
  }

  async function handleConfirmSend() {
    if (!confirmingSend) {
      setConfirmingSend(true);
      return;
    }
    setSending(true);
    setError(null);
    try {
      const savedOk = await saveEdits();
      if (!savedOk) return;
      const res = await fetch(`/api/redesigns/${redesignId}/proposal/send`, { method: "POST" });
      const data = await res.json();
      if (!res.ok) {
        setError(data.error ?? "Envio falhou");
        return;
      }
      setProposal((prev) => (prev ? { ...prev, email_sent_at: new Date().toISOString() } : prev));
    } catch {
      setError("Erro de conexão. Tenta de novo.");
    } finally {
      setSending(false);
      setConfirmingSend(false);
    }
  }

  if (!isPublic) {
    return (
      <section className="bg-card border border-border rounded-2xl p-6">
        <h2 className="font-display font-bold text-ink mb-2">Proposta</h2>
        <p className="text-sm text-muted">Publique o redesign primeiro pra gerar a proposta.</p>
      </section>
    );
  }

  if (!proposal) {
    return (
      <section className="bg-card border border-border rounded-2xl p-6">
        <h2 className="font-display font-bold text-ink mb-4">Proposta</h2>
        <button
          onClick={handleGenerate}
          disabled={generating}
          className="inline-flex items-center gap-1.5 px-4 py-2.5 rounded-xl bg-accent text-white text-sm font-medium hover:bg-accent-dim transition-colors disabled:opacity-50"
        >
          {generating ? <Loader2 size={14} className="animate-spin" /> : <Sparkles size={14} />}
          Gerar proposta
        </button>
        {error && <p className="text-sm text-bad mt-2">{error}</p>}
      </section>
    );
  }

  const emailBlocked = suppression
    ? suppression.opted_out_at
      ? "Este negócio pediu pra não receber contatos automáticos"
      : "Já contatado por e-mail (por você ou outro assinante)"
    : null;

  return (
    <section className="bg-card border border-border rounded-2xl p-6 space-y-4">
      <h2 className="font-display font-bold text-ink">Proposta</h2>

      <div>
        <label className="block text-sm text-muted mb-1">Assunto do e-mail</label>
        <input
          value={emailSubject}
          onChange={(e) => setEmailSubject(e.target.value)}
          className="w-full px-4 py-2.5 rounded-xl border border-border bg-surface text-ink focus:outline-none focus:ring-2 focus:ring-accent/40"
        />
      </div>
      <div>
        <label className="block text-sm text-muted mb-1">Corpo do e-mail</label>
        <textarea
          value={emailBody}
          onChange={(e) => setEmailBody(e.target.value)}
          rows={6}
          className="w-full px-4 py-2.5 rounded-xl border border-border bg-surface text-ink focus:outline-none focus:ring-2 focus:ring-accent/40"
        />
      </div>
      <div>
        <label className="block text-sm text-muted mb-1">Texto para WhatsApp</label>
        <textarea
          value={whatsappText}
          onChange={(e) => setWhatsappText(e.target.value)}
          rows={4}
          className="w-full px-4 py-2.5 rounded-xl border border-border bg-surface text-ink focus:outline-none focus:ring-2 focus:ring-accent/40"
        />
      </div>

      {error && <p className="text-sm text-bad">{error}</p>}

      <div className="flex items-center gap-3 flex-wrap pt-2 border-t border-border">
        <button
          onClick={saveEdits}
          disabled={saving}
          className="inline-flex items-center gap-1.5 px-4 py-2.5 rounded-xl border border-border text-sm font-medium text-ink hover:border-accent hover:text-accent transition-colors disabled:opacity-50"
        >
          {saving ? <Loader2 size={14} className="animate-spin" /> : <Save size={14} />}
          Salvar edição
        </button>

        {phone && (
          <a
            href={toWhatsAppLink(phone, whatsappText)}
            target="_blank"
            rel="noopener noreferrer"
            className="inline-flex items-center gap-1.5 px-4 py-2.5 rounded-xl bg-good text-white text-sm font-medium hover:opacity-90 transition-opacity"
          >
            <MessageCircle size={14} /> Enviar por WhatsApp
          </a>
        )}

        {publicEmail && (
          <button
            onClick={handleConfirmSend}
            onBlur={() => setConfirmingSend(false)}
            disabled={sending || Boolean(emailBlocked)}
            title={emailBlocked ?? undefined}
            className={
              confirmingSend
                ? "inline-flex items-center gap-1.5 px-4 py-2.5 rounded-xl bg-bad text-white text-sm font-medium disabled:opacity-50"
                : "inline-flex items-center gap-1.5 px-4 py-2.5 rounded-xl bg-accent text-white text-sm font-medium hover:bg-accent-dim transition-colors disabled:opacity-50"
            }
          >
            {sending ? <Loader2 size={14} className="animate-spin" /> : <Mail size={14} />}
            {emailBlocked ?? (confirmingSend ? "Confirmar envio?" : "Enviar por e-mail")}
          </button>
        )}

        {!phone && !publicEmail && (
          <p className="text-sm text-muted">Sem WhatsApp nem e-mail disponíveis — copie o texto manualmente.</p>
        )}
      </div>

      {proposal.email_sent_at && <p className="text-sm text-good">E-mail enviado ✓</p>}
    </section>
  );
}
```

Decisão de design importante: `handleConfirmSend` salva as edições (`saveEdits()`) ANTES de chamar a rota de envio -- a rota de envio (`/proposal/send`) lê o texto de dentro da tabela `proposals`, não do corpo da requisição, então sem esse salvamento prévio um texto editado na tela mas não salvo seria enviado com o conteúdo antigo. Isso garante que o que está na tela É o que sai no e-mail, sem exigir um clique extra de "salvar" antes de "enviar".
  </action>
  <verify>grep -q "export function ProposalSection" "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx" && grep -q "confirmingSend" "app/painel/leads/[leadId]/redesenhar/proposal-section.tsx"</verify>
  <done>Componente cobre os 3 estados (não publicado / sem proposta / proposta gerada), esconde WhatsApp/e-mail por dado faltante, desabilita e-mail por supressão com mensagem, confirmação em dois cliques antes de enviar.</done>
</task>

<task type="auto">
  <name>Task 3: Wire em page.tsx -- queries novas + renderização</name>
  <files>app/painel/leads/[leadId]/redesenhar/page.tsx</files>
  <action>
Editar `app/painel/leads/[leadId]/redesenhar/page.tsx`:

1. Import: adicionar `import { ProposalSection } from "./proposal-section";`

2. Trocar o select de `lead` (hoje `.select("id, place_id, status")`) por:
```ts
.select("id, place_id, status, public_email")
```

3. No `Promise.all` que já busca `quota` e `redesign`, adicionar uma terceira busca de supressão (sempre roda, é barata e `lead.place_id` já está disponível neste ponto):
```ts
const [quota, { data: redesign }, { data: suppression }] = await Promise.all([
  checkQuota(supabase, user.id, "redesign_generate"),
  supabase
    .from("redesigns")
    .select("id, content, before_screenshot_url, created_at, is_public, public_slug")
    .eq("lead_id", leadId)
    .order("created_at", { ascending: false })
    .limit(1)
    .maybeSingle(),
  supabase
    .from("contacted_businesses")
    .select("opted_out_at")
    .eq("place_id", lead.place_id)
    .maybeSingle(),
]);
```

4. Depois desse bloco, SE `redesign` existir, buscar a proposta já existente (depende de `redesign.id`, por isso não entra no `Promise.all` acima):
```ts
const { data: proposal } = redesign
  ? await supabase
      .from("proposals")
      .select("id, email_subject, email_body, whatsapp_text, email_sent_at")
      .eq("redesign_id", redesign.id)
      .maybeSingle()
  : { data: null };
```

5. Dentro do bloco `{!redesign ? (...) : (<div className="space-y-6">...)}`, depois de `<PublishButton .../>` e ANTES da `<div className="pt-2 border-t border-border" />` seguinte de GenerateButton/Editar/Excluir, adicionar:
```tsx
<ProposalSection
  redesignId={redesign.id}
  isPublic={redesign.is_public}
  phone={(redesign.content as RedesignContent).facts.phone}
  publicEmail={lead.public_email}
  initialProposal={proposal}
  suppression={suppression}
/>
```

Não remover nem reordenar nenhum outro elemento da página -- só adicionar a query de `suppression`/`proposal` e o componente novo.
  </action>
  <verify>grep -q "ProposalSection" "app/painel/leads/[leadId]/redesenhar/page.tsx" && grep -q "contacted_businesses" "app/painel/leads/[leadId]/redesenhar/page.tsx" && npm run build</verify>
  <done>page.tsx busca public_email/suppression/proposal e renderiza ProposalSection quando o redesign existe; build compila.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] Redesign não publicado: seção de proposta mostra só o aviso "publique primeiro", sem botão de gerar
- [ ] Redesign publicado, sem proposta: botão "Gerar proposta" aparece
- [ ] Proposta gerada: textareas de assunto/corpo/whatsapp editáveis, botão "Salvar edição" funcional
- [ ] `content.facts.phone` nulo → botão de WhatsApp não aparece (não desabilitado -- ausente)
- [ ] `lead.public_email` nulo → botão de e-mail não aparece
- [ ] `contacted_businesses` tem linha pro `place_id` do lead → botão de e-mail aparece DESABILITADO com mensagem distinguindo "já contatado" de "pediu pra não receber"; WhatsApp continua ativo
- [ ] Clicar "Enviar por e-mail" exige um segundo clique de confirmação antes de disparar a rota de envio
</verification>

<success_criteria>
- Todas as tasks completas
- Build limpo
- As 4 requirements da Fase 5 (PROPOSTA-01 a 04) verificáveis de ponta a ponta na UI: gerar → editar → WhatsApp/e-mail → supressão bloqueando reenvio
</success_criteria>

<output>
Após completar, criar `.planning/phases/05-proposta/05-06-SUMMARY.md`
</output>
