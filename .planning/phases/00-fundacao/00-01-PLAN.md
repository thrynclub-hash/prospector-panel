---
phase: 00-fundacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [supabase/migrations/20260708000000_baseline.sql, app/painel/layout.tsx, app/painel/page.tsx]
autonomous: true
requirements: [FOUND-01, FOUND-02]
user_setup: []

must_haves:
  truths:
    - "Uma página nova sob app/painel/ fica protegida automaticamente sem escrever checagem de auth de novo"
    - "Assinante com sessão válida mas status != active é redirecionado pra /obrigado, não vê o painel"
    - "O schema de prospector_customers está descrito em supabase/migrations/, não só no dashboard"
  artifacts:
    - path: "supabase/migrations/20260708000000_baseline.sql"
      provides: "Baseline versionado do schema real de prospector_customers"
      contains: "create table"
    - path: "app/painel/layout.tsx"
      provides: "Guard compartilhado de sessão + status para toda a subárvore /painel"
      contains: "redirect"
  key_links:
    - from: "app/painel/layout.tsx"
      to: "prospector_customers.status"
      via: "query Supabase por status ativo antes de renderizar children"
      pattern: "status.*active"
---

<objective>
Fechar as duas dívidas já mapeadas em `.planning/codebase/CONCERNS.md` antes que as próximas 5 fases (Buscar/Redesenhar/Editor/Publicar/Proposta) construam mais páginas e tabelas em cima delas.

Purpose: sem isso, cada página nova do roadmap repetiria a checagem de auth manualmente (e provavelmente esqueceria de checar status, não só sessão), e o schema continuaria existindo só na cabeça de quem mexeu no dashboard Supabase.
Output: uma migration SQL versionada capturando o schema real de `prospector_customers`, e um `app/painel/layout.tsx` que vira o único lugar que precisa saber como proteger o painel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-fundacao/00-RESEARCH.md
@app/painel/page.tsx
@lib/supabase/server.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migration baseline do schema real</name>
  <files>supabase/migrations/20260708000000_baseline.sql</files>
  <action>
Criar o diretório `supabase/migrations/` (não existe ainda) e o arquivo de migration abaixo, capturando EXATAMENTE o schema introspectado em 00-RESEARCH.md — não inventar colunas, não remover as colunas `zapi_*` mesmo não sendo usadas por este projeto (são do ZapFlow, banco compartilhado; remover está fora de escopo desta fase).

Conteúdo:
```sql
-- Baseline: captura o schema real de prospector_customers como ele existe hoje
-- no banco Supabase compartilhado (projeto "Marusso Projetos"), antes de este
-- projeto começar a versionar migrations. Colunas zapi_* pertencem ao ZapFlow
-- (mesmo banco compartilhado) -- mantidas aqui porque já existem na tabela real,
-- não porque este projeto as usa.
create table if not exists prospector_customers (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id),
  email text not null,
  kiwify_order_id text,
  status text not null default 'active',
  zapi_instance_id text,
  zapi_token text,
  zapi_client_token text,
  created_at timestamptz not null default now()
);

comment on table prospector_customers is 'Compradores do painel Prospector de Sites - acesso liberado via webhook da Kiwify.';

-- O código da aplicação (app/api/kiwify/webhook/route.ts) já trata email como
-- único (busca por .eq("email", ...).maybeSingle() antes de criar). Esta
-- constraint só torna explícito no banco o que o código já pressupõe.
create unique index if not exists prospector_customers_email_key on prospector_customers (email);
```

Usar `create table if not exists` e `create unique index if not exists` (não `create table` puro) porque a tabela JÁ EXISTE no banco real com dados reais -- esta migration documenta o estado atual pra versionamento futuro, não recria do zero. Rodar `if not exists` evita erro ao aplicar contra o banco que já tem a tabela.
  </action>
  <verify>test -f supabase/migrations/20260708000000_baseline.sql && grep -q "create table if not exists prospector_customers" supabase/migrations/20260708000000_baseline.sql</verify>
  <done>Arquivo de migration existe, reflete as 9 colunas reais confirmadas na pesquisa, usa IF NOT EXISTS pra ser seguro contra o banco que já tem a tabela</done>
</task>

<task type="auto">
  <name>Task 2: Criar guard compartilhado em app/painel/layout.tsx</name>
  <files>app/painel/layout.tsx, app/painel/page.tsx</files>
  <action>
Criar `app/painel/layout.tsx` como Server Component (sem "use client") que:
1. Cria o client via `createSupabaseServerClient()` (mesmo import que `app/painel/page.tsx` já usa: `@/lib/supabase/server`)
2. Busca `supabase.auth.getUser()`. Sem `user` → `redirect("/login")`
3. Com `user`, consulta `prospector_customers` por `status` filtrando `.eq("user_id", user.id)` (não por email -- já tem o `user.id` da sessão) usando `.maybeSingle()`
4. Se não achou o customer OU `status !== "active"` → `redirect("/obrigado")` (reaproveita o fluxo self-service que já existe pra recuperar acesso)
5. Se passou nas duas checagens → `return &lt;&gt;{children}&lt;/&gt;;`

Depois, editar `app/painel/page.tsx` pra REMOVER a checagem de auth que já existe lá (o `const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login");`) -- essa lógica agora vive só no layout. A página continua Server Component, só perde a parte de auth (o layout já garante que só chega até a página quem passou no guard). Manter o resto da UI da página exatamente como está.

Layout completo esperado:
```tsx
import { redirect } from "next/navigation";
import { createSupabaseServerClient } from "@/lib/supabase/server";

export default async function PainelLayout({ children }: { children: React.ReactNode }) {
  const supabase = await createSupabaseServerClient();
  const { data: { user } } = await supabase.auth.getUser();

  if (!user) {
    redirect("/login");
  }

  const { data: customer } = await supabase
    .from("prospector_customers")
    .select("status")
    .eq("user_id", user.id)
    .maybeSingle();

  if (!customer || customer.status !== "active") {
    redirect("/obrigado");
  }

  return <>{children}</>;
}
```
  </action>
  <verify>npm run build (deve compilar sem erro de tipo) e grep -q "redirect" app/painel/layout.tsx</verify>
  <done>app/painel/layout.tsx existe e faz as duas checagens (sessão + status); app/painel/page.tsx não tem mais lógica de auth duplicada; build passa limpo</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `app/painel/layout.tsx` existe, é Server Component, redireciona pra `/login` sem sessão e pra `/obrigado` sem assinatura ativa
- [ ] `app/painel/page.tsx` não duplica mais a checagem de auth
- [ ] `supabase/migrations/20260708000000_baseline.sql` existe e usa `IF NOT EXISTS`
- [ ] Teste manual: usuário já liberado (thrynclub@gmail.com, criado nesta sessão) continua acessando `/painel` normalmente depois da mudança
</verification>

<success_criteria>
- Todas as tasks completas
- Build limpo
- `/painel` continua funcionando pra quem já tem acesso, e passa a bloquear quem não tem assinatura ativa (não só sessão)
</success_criteria>

<output>
Após completar, criar `.planning/phases/00-fundacao/00-01-SUMMARY.md`
</output>
