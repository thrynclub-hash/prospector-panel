---
phase: 02.2-redesign-estrutura-editor-inline
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["app/painel/leads/[leadId]/redesenhar/editable-field.tsx", "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx", "app/painel/leads/[leadId]/redesenhar/editar/page.tsx", "app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "RedesignPreview ganha hero de 2 colunas em desktop (texto+foto) com 2 CTAs (WhatsApp sólido cor de marca + 'Ver serviços' outline) e card de nota flutuante sobre a foto -- não mais o hero centralizado de 1 coluna atual"
    - "Serviços viram cards clicáveis pro WhatsApp (wa.me via toWhatsAppLink reaproveitado de lib/proposal/whatsapp-link.ts) só no modo LEITURA (canEdit=false) -- no modo edição o card não é um link, pra não competir com o clique-pra-editar"
    - "Nova seção 'Localização e Contato' (id=localizacao): mapa embed do Google Maps (iframe https://www.google.com/maps?q={endereco}&output=embed, sem API key), endereço, telefone, horário de funcionamento (facts.openingHours, só se presente -- Plan 01), CTA final de WhatsApp -- só renderiza se houver endereço OU telefone"
    - "Botão WhatsApp flutuante (fixed bottom-right) e nav simples no topo (logo+nome+links âncora) -- só renderizam elementos de WhatsApp quando facts.phone existe (nunca fabricar um contato que não existe)"
    - "Tipografia Playfair Display (headings, via style inline) + Inter (corpo, via style inline no wrapper) carregada com next/font/google e escopada só a este componente (CSS custom properties --rp-display/--rp-body) -- a identidade Unbounded/Manrope do painel (app/layout.tsx) nunca é usada aqui, e a regra global `h1,h2,h3{font-family:var(--font-display)}` de app/globals.css é vencida por style inline em cada heading (nunca editado globals.css)"
    - "content.facts (nome, categoria, endereço, telefone, nota, avaliações, horário) NUNCA é editável -- só content.generated (heroHeadline, heroSubheadline, aboutCopy, services[].title/description) e content.photos (logoUrl, photoUrls) ficam editáveis quando canEdit=true"
    - "Editor inline: clicar num texto edita no local (contentEditable) e salva sozinho no blur via PATCH /api/redesigns/[id] já existente (generated completo + photos completo, nunca parcial); clicar numa imagem abre seletor de arquivo e faz upload via POST /api/redesigns/[id]/photo já existente, atualizando o preview e persistindo via PATCH -- sem botão 'Salvar', cada edição já fica salva"
    - "editable/redesignId são opcionais, default editable=false -- os 4 call sites que NÃO editam (Comparator, /compare, /preview, /demo/[slug]) continuam funcionando sem nenhuma alteração de código, renderizando exatamente como hoje (foto+texto, sem contentEditable, sem chamadas de rede)"
    - "/editar (Fase 3) deixa de renderizar o formulário (editor-form.tsx) e passa a renderizar RedesignPreview com editable+redesignId -- editor-form.tsx é removido do projeto (superseded), rota/entrada (/editar) permanece a mesma"
    - "Cap de fotos consumido do preview é o que vier de content.photos.photoUrls (até 8, Plan 01) -- 1a foto no hero, as demais numa seção de Galeria"
  artifacts:
    - path: "app/painel/leads/[leadId]/redesenhar/editable-field.tsx"
      provides: "EditableText (contentEditable + onBlur salva) e EditableImage (clique abre file input + upload) -- client components reusáveis"
      contains: "export function EditableText"
    - path: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      provides: "Página redesenhada completa: nav, hero 2-col+2 CTAs+card flutuante, barra de confiança, serviços->WhatsApp, sobre, galeria, localização+mapa+horário, WhatsApp flutuante -- tudo editável quando canEdit"
      contains: "id=\"localizacao\""
    - path: "app/painel/leads/[leadId]/redesenhar/editar/page.tsx"
      provides: "Renderiza RedesignPreview com editable+redesignId no lugar do form antigo"
      contains: "editable redesignId={redesign.id}"
  key_links:
    - from: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      to: "app/painel/leads/[leadId]/redesenhar/editable-field.tsx"
      via: "EditableText/EditableImage usados pra todo campo de generated/photos"
      pattern: "from \"./editable-field\""
    - from: "app/painel/leads/[leadId]/redesenhar/editable-field.tsx"
      to: "app/api/redesigns/[id]/route.ts"
      via: "EditableText dispara saveGenerated -> persist() -> PATCH /api/redesigns/{id} com generated+photos completos"
      pattern: "fetch\\(`/api/redesigns/\\$\\{redesignId\\}`"
    - from: "app/painel/leads/[leadId]/redesenhar/editable-field.tsx"
      to: "app/api/redesigns/[id]/photo/route.ts"
      via: "EditableImage faz upload via POST /api/redesigns/[id]/photo (rota já existente da Fase 3), depois PATCH persiste a nova URL"
      pattern: "fetch\\(`/api/redesigns/\\$\\{redesignId\\}/photo`"
    - from: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      to: "lib/proposal/whatsapp-link.ts"
      via: "toWhatsAppLink reaproveitado (Fase 5) pros CTAs de hero/serviços/localização/botão flutuante"
      pattern: "toWhatsAppLink\\("
    - from: "app/painel/leads/[leadId]/redesenhar/editar/page.tsx"
      to: "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"
      via: "editable+redesignId habilita o modo edição só nesta rota"
      pattern: "editable redesignId="
---

<objective>
02.2-CONTEXT.md (seções "O que muda na estrutura da página gerada" e "Editor inline") pede duas entregas combinadas porque usam o MESMO componente de renderização: (1) elevar a estrutura/design de `RedesignPreview` ao padrão real da skill `redesign-premium` (hero com 2 CTAs, seção de localização/mapa/horário, mais fotos, tipografia Playfair+Inter -- não só cor/logo/foto, que já foi resolvido na Fase 02.1), e (2) trocar o Editor (Fase 3, formulário separado) por edição INLINE direto no preview (clicar-editar texto, clicar-trocar imagem), replicando a UX do plugin original (`references/editor-visual.md`) adaptada pro modelo React/Next.js deste projeto.

Depende de 02.2-01-PLAN.md: usa `facts.openingHours` (novo campo) e assume até 8 fotos em `photos.photoUrls` (cap subiu de 3).

**Por que os dois numa passada só**: fazer a estrutura primeiro (sem editor) e depois voltar pra adicionar contentEditable em cada elemento significaria escrever o mesmo JSX duas vezes -- o mecanismo de edição (EditableText/EditableImage) precisa envolver exatamente os mesmos elementos que a nova estrutura cria (hero headline, subheadline, serviços, sobre, fotos). Uma passada só é o caminho de menor risco de inconsistência.

**Decisões desta sessão de planejamento (Claude's Discretion, 02.2-CONTEXT.md deixou em aberto)**:
1. **CTA 2 do hero**: o exemplo de referência (screenshot `camila-fontes-editor.html`) usa "Como funciona" -- mas este produto não tem uma fonte real de "como funciona o atendimento" pra essa clínica específica (só facts verificados + serviços). Pra não fabricar uma narrativa de processo que não existe na fonte (espírito da regra 1 da skill, mesmo não sendo um "fato" no sentido estrito), o CTA 2 vira **"Ver serviços"**, âncora real pra `#servicos` -- uma ação navegável de verdade, não um texto vazio.
2. **`/editar` é mantido, não removido** -- em vez de apagar a rota, seu CONTEÚDO troca: o formulário (`editor-form.tsx`) é substituído pelo preview real com edição inline. Isso preserva toda a navegação existente (botão "Editar" na página principal continua funcionando, sem precisar tocar em `redesenhar/page.tsx`) e mapeia mais fielmente pro par `index.html`/`[slug]-editor.html` do plugin original: `/demo/[slug]` = publicado/leitura, `/editar` = modo edição -- exatamente essa dualidade.
3. **`position: fixed` no botão de WhatsApp flutuante**: correto pro contexto que importa de verdade (`/demo/[slug]`, visto por clientes reais, e `/preview`/`/editar`, páginas cheias) -- mas cosmeticamente pode ficar "grudado" no canto real do navegador (não do container) quando `RedesignPreview` é embutido dentro de uma caixa com scroll próprio menor que a tela inteira (`Comparator`, 600px; `/compare`, coluna de 50%). Isso é um artefato cosmético aceitável nesses 2 contextos internos (uso do assinante comparando, nunca visto pelo cliente final) -- não vale a complexidade de um prop extra só pra esse detalhe.
4. **Cap de fotos consumido aqui**: o que vier de `content.photos.photoUrls` (até 8 depois do Plan 01) -- a 1a foto vai pro hero, as demais pra uma seção "Fotos" (galeria) mais abaixo.

**Escopo deliberadamente fora desta fase** (documentar pra não ser re-adicionado sem essa discussão de novo): o editor inline permite *trocar* uma foto existente (clicar → seletor de arquivo → upload → substitui), replicando exatamente `references/editor-visual.md` -- **não** permite adicionar uma foto nova nem remover uma foto da galeria (capacidades que existiam no formulário antigo, `editor-form.tsx`). O plugin original também só suporta trocar `<img>` já existentes, nunca adicionar/remover -- esta fase prioriza replicar exatamente o comportamento do plugin sobre preservar um superset de capacidades do formulário que está sendo substituído.

Purpose: fazer o redesign gerado e o modo de edição corresponderem de verdade ao "site de R$10 mil" que a skill `redesign-premium` descreve, e à UX exata do plugin original que o usuário pediu pra replicar.
Output: `RedesignPreview` reescrito (estrutura completa + tipografia própria + edição inline opcional); `/editar` renderizando o preview real em vez de um formulário; os outros 4 call sites (Comparator, /compare, /preview, /demo/[slug]) inalterados.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.2-redesign-estrutura-editor-inline/02.2-CONTEXT.md
@types/redesign-content.ts
@app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx
@app/painel/leads/[leadId]/redesenhar/editar/page.tsx
@app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx
@app/painel/leads/[leadId]/redesenhar/comparator.tsx
@app/painel/leads/[leadId]/redesenhar/compare/page.tsx
@app/painel/leads/[leadId]/redesenhar/preview/page.tsx
@app/demo/[slug]/page.tsx
@app/api/redesigns/[id]/route.ts
@app/api/redesigns/[id]/photo/route.ts
@lib/proposal/whatsapp-link.ts
@app/layout.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar editable-field.tsx (EditableText + EditableImage)</name>
  <files>app/painel/leads/[leadId]/redesenhar/editable-field.tsx</files>
  <action>
Criar `app/painel/leads/[leadId]/redesenhar/editable-field.tsx` -- client components reusáveis que adaptam o mecanismo de `references/editor-visual.md` (contenteditable + input file escondido) pro modelo React: em vez de um botão "Exportar" no final, cada campo salva sozinho (`onBlur`/upload) via as rotas que já existem.

```tsx
"use client";

import { useRef, useState } from "react";
import { Loader2, Pencil } from "lucide-react";

// Camada de edição inline compartilhada por RedesignPreview (Fase 02.2) --
// adaptação do mecanismo do plugin original (references/editor-visual.md:
// contenteditable + input file escondido, botão único de "Exportar" no
// final) pro modelo React/Next.js deste projeto: cada campo salva sozinho
// (onBlur / upload) via as rotas que já existem (PATCH /api/redesigns/[id],
// POST /api/redesigns/[id]/photo) -- não há "modo exportação", o dado já
// fica persistido no banco a cada edição, não num arquivo baixado.

type EditableTag = "h1" | "h2" | "h3" | "p" | "span";

export function EditableText({
  value,
  editable,
  onSave,
  as: Tag = "span",
  className,
  style,
  multiline = false,
}: {
  value: string;
  editable: boolean;
  onSave: (next: string) => void;
  as?: EditableTag;
  className?: string;
  style?: React.CSSProperties;
  multiline?: boolean;
}) {
  const mergedStyle: React.CSSProperties = { ...style, whiteSpace: multiline ? "pre-wrap" : undefined };

  if (!editable) {
    return <Tag className={className} style={mergedStyle}>{value}</Tag>;
  }

  return (
    <Tag
      className={`${className ?? ""} cursor-text rounded outline-none hover:ring-2 hover:ring-blue-400/70 focus:ring-2 focus:ring-blue-500`}
      style={mergedStyle}
      contentEditable
      suppressContentEditableWarning
      onKeyDown={(e) => {
        // Título/label de serviço é 1 linha só -- Enter não deve quebrar em
        // parágrafo. Campos multiline (Sobre, descrição de serviço) permitem
        // Enter normalmente.
        if (e.key === "Enter" && !multiline) e.preventDefault();
      }}
      onBlur={(e) => {
        // innerText (não textContent) preserva quebras de linha visuais
        // corretamente em campos multiline.
        const next = e.currentTarget.innerText.trim();
        if (next && next !== value) onSave(next);
      }}
    >
      {value}
    </Tag>
  );
}

export function EditableImage({
  src,
  alt,
  editable,
  className,
  redesignId,
  onUploaded,
}: {
  src: string;
  alt: string;
  editable: boolean;
  /** Classes de tamanho/arredondamento (ex: "w-full h-72 rounded-2xl") -- object-fit é sempre cover, aplicado internamente. */
  className?: string;
  redesignId?: string;
  onUploaded: (url: string) => void;
}) {
  const inputRef = useRef<HTMLInputElement>(null);
  const [uploading, setUploading] = useState(false);

  if (!editable || !redesignId) {
    // eslint-disable-next-line @next/next/no-img-element
    return <img src={src} alt={alt} className={`${className ?? ""} object-cover`} />;
  }

  async function handleFile(e: React.ChangeEvent<HTMLInputElement>) {
    const file = e.target.files?.[0];
    if (!file) return;
    setUploading(true);
    try {
      const formData = new FormData();
      formData.append("file", file);
      const res = await fetch(`/api/redesigns/${redesignId}/photo`, { method: "POST", body: formData });
      const data = await res.json();
      if (res.ok && data.url) onUploaded(data.url);
    } catch {
      // Upload falhou -- imagem atual permanece, usuário pode clicar de novo.
    } finally {
      setUploading(false);
      e.target.value = "";
    }
  }

  return (
    <button
      type="button"
      onClick={() => inputRef.current?.click()}
      className={`relative block overflow-hidden border-0 bg-transparent p-0 group ${className ?? ""}`}
      aria-label="Trocar imagem"
    >
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src={src} alt={alt} className="w-full h-full object-cover" />
      <span className="pointer-events-none absolute inset-0 flex items-center justify-center bg-black/0 opacity-0 transition-colors group-hover:bg-black/30 group-hover:opacity-100">
        {uploading ? <Loader2 className="animate-spin text-white" size={20} /> : <Pencil className="text-white" size={20} />}
      </span>
      <input ref={inputRef} type="file" accept="image/*" onChange={handleFile} className="hidden" />
    </button>
  );
}
```
  </action>
  <verify>grep -q "export function EditableText" "app/painel/leads/[leadId]/redesenhar/editable-field.tsx" && grep -q "export function EditableImage" "app/painel/leads/[leadId]/redesenhar/editable-field.tsx"</verify>
  <done>Ambos componentes criados; modo não-editável renderiza texto/imagem plana (sem contentEditable/upload); modo editável adiciona a interação sem exigir redesignId quando ausente (degrada pra leitura).</done>
</task>

<task type="auto">
  <name>Task 2: Reescrever redesign-preview.tsx -- estrutura redesign-premium + tipografia própria + edição inline</name>
  <files>app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx</files>
  <action>
Substituir TODO o conteúdo de `app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx`. Vira `"use client"` (necessário pra contentEditable/onClick/useState) -- isso é seguro pros 4 call sites Server Component existentes (Comparator, /compare, /preview, /demo/[slug]) porque Next.js renderiza Client Components normalmente quando importados por um Server Component, sem exigir nenhuma mudança neles.

```tsx
"use client";

import { useState } from "react";
import { Playfair_Display, Inter } from "next/font/google";
import { MessageCircle } from "lucide-react";
import type { RedesignContent } from "@/types/redesign-content";
import { toWhatsAppLink } from "@/lib/proposal/whatsapp-link";
import { EditableText, EditableImage } from "./editable-field";

// Estrutura elevada ao padrão da skill redesign-premium (hero com 2 CTAs +
// card de nota flutuante, barra de confiança, serviços clicáveis pro
// WhatsApp, seção de Localização e Contato com mapa+horário, botão WhatsApp
// flutuante, nav simples) -- 02.2-CONTEXT.md.
//
// Tipografia PRÓPRIA deste componente: Playfair Display (headings) + Inter
// (corpo), escolhida via ui-ux-pro-max como par editorial adequado a negócio
// local premium -- NUNCA a identidade "Signal Ledger" do painel (Unbounded/
// Manrope, app/layout.tsx: essa é a marca do PRODUTO, não do negócio do
// lead -- skill redesign-premium regra 3). app/globals.css define
// `h1,h2,h3{font-family:var(--font-display)}` globalmente -- um `style`
// inline em cada heading é a única forma de vencer essa regra sem tocar em
// globals.css (valor inline sempre ganha de uma regra de elemento no CSS
// cascade, mesmo de baixa especificidade).
//
// Editor inline (Fase 02.2, substitui o formulário da Fase 3): quando
// `editable` + `redesignId`, cada campo de `generated` vira contentEditable
// (EditableText) e cada imagem de `photos` vira clicável-pra-trocar
// (EditableImage) -- ver editable-field.tsx. `facts` (nome, endereço,
// telefone, nota, horário) NUNCA é editável aqui -- mesma regra da Fase
// 3/02.1 (snapshot verificado do Places; editar sem controle abriria brecha
// pra "corrigir" um fato pra algo não verificado).
//
// Componente compartilhado por 5 contextos: Comparator (preview privado),
// /compare, /preview, /demo/[slug] (público) -- todos com editable=false
// (default, nenhuma mudança de código neles) -- e /editar, o único que passa
// editable+redesignId.
const playfair = Playfair_Display({ subsets: ["latin"], weight: ["600", "700"], variable: "--rp-display", display: "swap" });
const inter = Inter({ subsets: ["latin"], weight: ["400", "500", "600"], variable: "--rp-body", display: "swap" });

const displayFont = { fontFamily: "var(--rp-display)" } as const;

export function RedesignPreview({
  content,
  editable = false,
  redesignId,
}: {
  content: RedesignContent;
  editable?: boolean;
  redesignId?: string;
}) {
  const [generated, setGenerated] = useState(content.generated);
  const [photos, setPhotos] = useState(content.photos);
  const { facts, theme } = content; // read-only sempre -- nunca setState aqui
  const accentColor = theme?.primaryColor ?? null;
  const canEdit = editable && Boolean(redesignId);

  const whatsappBase = facts.phone
    ? toWhatsAppLink(facts.phone, `Olá! Vim pelo site da ${facts.name} e quero saber mais.`)
    : null;

  async function persist(next: { generated?: RedesignContent["generated"]; photos?: RedesignContent["photos"] }) {
    if (!redesignId) return;
    await fetch(`/api/redesigns/${redesignId}`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        generated: next.generated ?? generated,
        photos: next.photos ?? photos,
      }),
    }).catch(() => {
      // Falha de rede na autosave -- estado local já refletiu a edição (UX
      // não trava); a próxima edição bem-sucedida reenvia o estado completo
      // mais atual de qualquer forma.
    });
  }

  function saveGenerated(next: RedesignContent["generated"]) {
    setGenerated(next);
    void persist({ generated: next });
  }

  function saveLogo(url: string) {
    const next = { ...photos, logoUrl: url };
    setPhotos(next);
    void persist({ photos: next });
  }

  function savePhotoAt(index: number, url: string) {
    const next = { ...photos, photoUrls: photos.photoUrls.map((u, i) => (i === index ? url : u)) };
    setPhotos(next);
    void persist({ photos: next });
  }

  const heroPhoto = photos.photoUrls[0] ?? null;
  const galleryPhotos = photos.photoUrls.slice(1);
  const hasLocationSection = Boolean(facts.address) || Boolean(whatsappBase);

  return (
    <div
      className={`${playfair.variable} ${inter.variable} relative bg-white text-slate-900`}
      style={{ fontFamily: "var(--rp-body)" }}
    >
      <nav className="sticky top-0 z-10 flex items-center justify-between gap-4 px-6 py-4 bg-white/95 backdrop-blur border-b border-slate-100">
        <div className="flex items-center gap-3 min-w-0">
          {photos.logoUrl ? (
            <EditableImage
              src={photos.logoUrl}
              alt={facts.name}
              editable={canEdit}
              redesignId={redesignId}
              onUploaded={saveLogo}
              className="h-8 w-8 rounded shrink-0"
            />
          ) : (
            <div className="h-8 w-8 shrink-0 rounded-full bg-slate-900 text-white flex items-center justify-center text-sm font-bold">
              {facts.name.slice(0, 1).toUpperCase()}
            </div>
          )}
          <span className="font-semibold truncate" style={displayFont}>
            {facts.name}
          </span>
        </div>
        <div className="hidden sm:flex items-center gap-6 text-sm text-slate-600 shrink-0">
          <a href="#servicos" className="hover:text-slate-900">
            Serviços
          </a>
          {hasLocationSection && (
            <a href="#localizacao" className="hover:text-slate-900">
              Localização
            </a>
          )}
        </div>
        {whatsappBase && (
          <a
            href={whatsappBase}
            target="_blank"
            rel="noopener noreferrer"
            className="shrink-0 px-4 py-2 rounded-full text-white text-sm font-medium"
            style={{ backgroundColor: accentColor ?? "#0f172a" }}
          >
            Agendar
          </a>
        )}
      </nav>

      <section className="grid md:grid-cols-2 gap-10 px-6 md:px-12 py-12 md:py-20 items-center max-w-6xl mx-auto">
        <div>
          {facts.category && (
            <p className="italic font-medium mb-3" style={{ color: accentColor ?? "#0f172a" }}>
              {facts.category}
            </p>
          )}
          <EditableText
            as="h1"
            editable={canEdit}
            value={generated.heroHeadline}
            onSave={(v) => saveGenerated({ ...generated, heroHeadline: v })}
            className="text-4xl md:text-5xl font-bold leading-tight mb-4"
            style={displayFont}
          />
          <EditableText
            as="p"
            editable={canEdit}
            value={generated.heroSubheadline}
            onSave={(v) => saveGenerated({ ...generated, heroSubheadline: v })}
            className="text-lg text-slate-600 mb-8"
          />
          <div className="flex flex-wrap gap-3">
            {whatsappBase && (
              <a
                href={whatsappBase}
                target="_blank"
                rel="noopener noreferrer"
                className="px-6 py-3 rounded-xl text-white font-medium"
                style={{ backgroundColor: accentColor ?? "#0f172a" }}
              >
                Falar no WhatsApp
              </a>
            )}
            <a
              href="#servicos"
              className="px-6 py-3 rounded-xl border font-medium border-slate-300 text-slate-700"
              style={accentColor ? { borderColor: accentColor, color: accentColor } : undefined}
            >
              Ver serviços
            </a>
          </div>
        </div>

        <div className="relative">
          {heroPhoto ? (
            <EditableImage
              src={heroPhoto}
              alt={facts.name}
              editable={canEdit}
              redesignId={redesignId}
              onUploaded={(url) => savePhotoAt(0, url)}
              className="w-full h-72 md:h-96 rounded-2xl"
            />
          ) : (
            <div className="w-full h-72 md:h-96 rounded-2xl bg-slate-100 flex items-center justify-center text-6xl font-bold text-slate-300">
              {facts.name.slice(0, 1).toUpperCase()}
            </div>
          )}
          {facts.rating && (
            <div className="absolute -bottom-4 left-4 bg-white rounded-xl shadow-lg px-4 py-3 text-sm">
              <span className="font-bold">⭐ {facts.rating}</span>
              {facts.userRatingCount ? <span className="text-slate-500"> · {facts.userRatingCount} avaliações</span> : null}
            </div>
          )}
        </div>
      </section>

      {(facts.rating || facts.category || facts.address) && (
        <div className="border-y border-slate-100 bg-slate-50">
          <div className="max-w-6xl mx-auto px-6 py-4 flex flex-wrap gap-x-8 gap-y-2 justify-center text-sm text-slate-600">
            {facts.rating && (
              <span>
                ⭐ {facts.rating} no Google{facts.userRatingCount ? ` (${facts.userRatingCount})` : ""}
              </span>
            )}
            {facts.category && <span>{facts.category}</span>}
            {facts.address && <span>{facts.address}</span>}
          </div>
        </div>
      )}

      <section id="servicos" className="px-6 md:px-12 py-16 max-w-6xl mx-auto">
        <h2 className="text-2xl font-bold mb-8 text-center" style={displayFont}>
          Serviços
        </h2>
        <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {generated.services.map((service, i) => {
            const serviceLink = facts.phone
              ? toWhatsAppLink(facts.phone, `Olá! Vim pelo site e quero saber sobre ${service.title}`)
              : null;

            const card = (
              <div className="h-full bg-white border border-slate-200 rounded-xl p-5 hover:shadow-md transition-shadow">
                <EditableText
                  as="p"
                  editable={canEdit}
                  value={service.title}
                  onSave={(v) =>
                    saveGenerated({
                      ...generated,
                      services: generated.services.map((s, si) => (si === i ? { ...s, title: v } : s)),
                    })
                  }
                  className="font-bold mb-2"
                />
                <EditableText
                  as="p"
                  editable={canEdit}
                  value={service.description}
                  onSave={(v) =>
                    saveGenerated({
                      ...generated,
                      services: generated.services.map((s, si) => (si === i ? { ...s, description: v } : s)),
                    })
                  }
                  className="text-sm text-slate-600"
                  multiline
                />
              </div>
            );

            // Card só vira link clicável no modo leitura -- no modo edição o
            // clique precisa cair no contentEditable, não navegar pro WhatsApp.
            return serviceLink && !canEdit ? (
              <a key={i} href={serviceLink} target="_blank" rel="noopener noreferrer" className="block">
                {card}
              </a>
            ) : (
              <div key={i}>{card}</div>
            );
          })}
        </div>
      </section>

      <section className="px-6 md:px-12 py-16 bg-slate-50">
        <div className="max-w-3xl mx-auto">
          <h2 className="text-2xl font-bold mb-4" style={displayFont}>
            Sobre
          </h2>
          <EditableText
            as="p"
            editable={canEdit}
            value={generated.aboutCopy}
            onSave={(v) => saveGenerated({ ...generated, aboutCopy: v })}
            className="text-slate-600 leading-relaxed"
            multiline
          />
        </div>
      </section>

      {galleryPhotos.length > 0 && (
        <section className="px-6 md:px-12 py-16 max-w-6xl mx-auto">
          <h2 className="text-2xl font-bold mb-8 text-center" style={displayFont}>
            Fotos
          </h2>
          <div className="grid grid-cols-2 sm:grid-cols-3 gap-3">
            {galleryPhotos.map((url, i) => (
              <EditableImage
                key={url}
                src={url}
                alt={facts.name}
                editable={canEdit}
                redesignId={redesignId}
                onUploaded={(newUrl) => savePhotoAt(i + 1, newUrl)}
                className="w-full h-40 rounded-xl"
              />
            ))}
          </div>
        </section>
      )}

      {hasLocationSection && (
        <section id="localizacao" className="px-6 md:px-12 py-16 bg-slate-900 text-white">
          <div className="max-w-6xl mx-auto grid md:grid-cols-2 gap-8 items-start">
            <div>
              <h2 className="text-2xl font-bold mb-6" style={displayFont}>
                Localização e Contato
              </h2>
              {facts.address && <p className="mb-2 text-slate-300">{facts.address}</p>}
              {facts.phone && <p className="mb-4 text-slate-300">{facts.phone}</p>}
              {facts.openingHours && facts.openingHours.length > 0 && (
                <ul className="mb-6 text-sm text-slate-400 space-y-1">
                  {facts.openingHours.map((line) => (
                    <li key={line}>{line}</li>
                  ))}
                </ul>
              )}
              {whatsappBase && (
                <a
                  href={whatsappBase}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-block px-6 py-3 rounded-xl text-white font-medium"
                  style={{ backgroundColor: accentColor ?? "#22c55e" }}
                >
                  Falar no WhatsApp
                </a>
              )}
            </div>
            {facts.address && (
              <iframe
                title={`Mapa -- ${facts.name}`}
                src={`https://www.google.com/maps?q=${encodeURIComponent(facts.address)}&output=embed`}
                className="w-full h-72 rounded-2xl border-0"
                loading="lazy"
              />
            )}
          </div>
        </section>
      )}

      {whatsappBase && (
        <a
          href={whatsappBase}
          target="_blank"
          rel="noopener noreferrer"
          className="fixed bottom-6 right-6 z-20 h-14 w-14 rounded-full bg-green-500 text-white flex items-center justify-center shadow-lg hover:bg-green-600 transition-colors"
          aria-label="Falar no WhatsApp"
        >
          <MessageCircle size={26} />
        </a>
      )}
    </div>
  );
}
```

Nota: `theme` é destructured de `content` mas só usado via `theme?.primaryColor` (linha seguinte) -- não é dead code.
  </action>
  <verify>grep -q "id=\"servicos\"" "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx" && grep -q "id=\"localizacao\"" "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx" && grep -q "from \"./editable-field\"" "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx" && grep -q "\"use client\"" "app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx"</verify>
  <done>RedesignPreview reescrito: nav simples, hero 2-col com 2 CTAs + card de nota flutuante, barra de confiança, serviços->WhatsApp (só leitura), sobre, galeria, localização+mapa+horário, WhatsApp flutuante. Tipografia Playfair+Inter escopada via style inline. facts nunca editável; generated/photos editáveis via EditableText/EditableImage quando canEdit. Os 4 call sites existentes (Comparator, /compare, /preview, /demo/[slug]) continuam passando só `content` -- editable/redesignId ficam undefined/false, então renderizam exatamente como antes.</done>
</task>

<task type="auto">
  <name>Task 3: editar/page.tsx -- renderizar o preview real com edição inline, no lugar do formulário</name>
  <files>app/painel/leads/[leadId]/redesenhar/editar/page.tsx</files>
  <action>
Substituir todo o conteúdo de `app/painel/leads/[leadId]/redesenhar/editar/page.tsx`. Mesma query de antes (busca `redesign` por `lead_id`+`user_id`); a única mudança é o que é renderizado: em vez de `<EditorForm redesignId={redesign.id} initialContent={content} />`, agora é `<RedesignPreview content={content} editable redesignId={redesign.id} />`, com uma barra fixa no topo explicando o modo de edição (mesmo espírito da barra `#pe-bar` do plugin original, `references/editor-visual.md`, adaptada -- sem botão "Exportar" porque aqui cada edição já salva sozinha).

```tsx
import Link from "next/link";
import { redirect, notFound } from "next/navigation";
import { ArrowLeft } from "lucide-react";
import { createSupabaseServerClient } from "@/lib/supabase/server";
import type { RedesignContent } from "@/types/redesign-content";
import { RedesignPreview } from "../redesign-preview";

// Fase 02.2: substitui o formulário (editor-form.tsx, Fase 3) pela edição
// inline direto no preview real -- clicar em texto edita no lugar, clicar em
// imagem troca via upload; cada edição salva sozinha (sem botão "Salvar
// edição" -- ver RedesignPreview/editable-field.tsx). A rota/entrada
// (/editar, o botão "Editar" na página principal) permanece a mesma; só o
// conteúdo interno muda -- nenhuma mudança em redesenhar/page.tsx.
export default async function EditarPage({ params }: { params: Promise<{ leadId: string }> }) {
  const { leadId } = await params;
  const supabase = await createSupabaseServerClient();
  const {
    data: { user },
  } = await supabase.auth.getUser();

  if (!user) {
    redirect("/login");
  }

  const { data: redesign } = await supabase
    .from("redesigns")
    .select("id, content")
    .eq("lead_id", leadId)
    .eq("user_id", user.id)
    .order("created_at", { ascending: false })
    .limit(1)
    .maybeSingle();

  if (!redesign) {
    notFound();
  }

  const content = redesign.content as RedesignContent;

  return (
    <div className="min-h-screen bg-bg">
      <div className="sticky top-0 z-30 bg-ink text-white px-4 py-2.5 text-xs sm:text-sm flex items-center gap-3 flex-wrap">
        <Link
          href={`/painel/leads/${leadId}/redesenhar`}
          className="inline-flex items-center gap-1.5 hover:opacity-80 shrink-0"
        >
          <ArrowLeft size={14} /> Voltar
        </Link>
        <span className="text-white/40">·</span>
        <span className="text-white/90">
          Modo edição — clique em qualquer texto ou foto abaixo pra editar direto. Dados verificados (nome, endereço,
          nota, horário) não são editáveis aqui.
        </span>
      </div>
      <RedesignPreview content={content} editable redesignId={redesign.id} />
    </div>
  );
}
```
  </action>
  <verify>grep -q "editable redesignId={redesign.id}" "app/painel/leads/[leadId]/redesenhar/editar/page.tsx" && ! grep -q "EditorForm" "app/painel/leads/[leadId]/redesenhar/editar/page.tsx"</verify>
  <done>editar/page.tsx renderiza RedesignPreview em modo edição; nenhuma referência a EditorForm sobra no arquivo.</done>
</task>

<task type="auto">
  <name>Task 4: Remover editor-form.tsx (superseded pelo editor inline)</name>
  <files>app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx</files>
  <action>
Apagar o arquivo `app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx` -- o formulário da Fase 3 foi substituído pela edição inline (Task 2+3 deste plano). Confirmado por grep antes de apagar: o único lugar que importava `EditorForm` era `editar/page.tsx`, já reescrito na Task 3.

```bash
git rm "app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx"
```

Se o arquivo não estiver rastreado pelo git (improvável, mas por segurança): `rm "app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx"`.
  </action>
  <verify>[ ! -f "app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx" ] && ! grep -rq "editor-form" app/ --include="*.tsx" --include="*.ts"</verify>
  <done>Arquivo removido; nenhuma referência a editor-form sobra no código.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` passa sem erros
- [ ] `app/painel/leads/[leadId]/redesenhar/editable-field.tsx`: `EditableText` e `EditableImage` existem, ambos degradam pra leitura simples quando `editable` é false (ou `redesignId` ausente, no caso de `EditableImage`)
- [ ] `app/painel/leads/[leadId]/redesenhar/redesign-preview.tsx`: `"use client"` no topo; seções `#servicos` e `#localizacao` existem; tipografia Playfair/Inter aplicada via `style` inline (nunca via className global); `facts` nunca passa por `setState`/`EditableText`/`EditableImage`
- [ ] `app/painel/leads/[leadId]/redesenhar/comparator.tsx`, `compare/page.tsx`, `preview/page.tsx`, `app/demo/[slug]/page.tsx`: nenhum desses 4 arquivos foi modificado (`git diff --stat` não deve listá-los) -- continuam passando só `content`, sem `editable`/`redesignId`
- [ ] `app/painel/leads/[leadId]/redesenhar/editar/page.tsx`: renderiza `<RedesignPreview content={content} editable redesignId={redesign.id} />`, zero referência a `EditorForm`
- [ ] `app/painel/leads/[leadId]/redesenhar/editar/editor-form.tsx`: não existe mais no filesystem
- [ ] Nenhuma migration SQL nova (esta plan não toca em schema -- isso foi 02.2-01-PLAN.md)
</verification>

<success_criteria>
- Todas as 4 tasks completas
- Build limpo (`npm run build`)
- `/editar` mostra o redesign real (não um formulário), com cada texto/imagem clicável pra editar, salvando sozinho sem botão "Salvar"
- `/demo/[slug]`, `/preview`, `/compare` e o Comparator embutido em `redesenhar/page.tsx` continuam exibindo a nova estrutura (hero 2 CTAs, localização/mapa/horário, etc.) em modo LEITURA -- sem contentEditable, sem chamadas de rede, sem crash por falta de `redesignId`
- Um redesign sem telefone (facts.phone null) não mostra nenhum elemento de WhatsApp (nem hero, nem serviços, nem localização, nem botão flutuante) -- degrada graciosamente, nunca fabrica um contato
- Um redesign sem endereço nem telefone não renderiza a seção "Localização e Contato" (hasLocationSection false)
</success_criteria>

<output>
Após completar, criar `.planning/phases/02.2-redesign-estrutura-editor-inline/02.2-02-SUMMARY.md`
</output>