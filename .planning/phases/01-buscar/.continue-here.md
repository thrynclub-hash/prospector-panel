---
phase: 01-buscar
task: 0
total_tasks: unknown
status: not_started
last_updated: 2026-07-08T01:09:03.483Z
---

<current_state>
Fase 0 (Fundação) está 100% completa, testada em produção, e commitada. Acabei de rodar `/gsd:plan-phase 1` e cheguei só até criar o diretório `.planning/phases/01-buscar/` — nenhuma pesquisa nem plano foi escrito ainda pra essa fase. Parei aqui porque o monitor de contexto avisou 81% de uso (35% restante) logo depois de criar o diretório, antes de eu começar a escrever `01-RESEARCH.md`.
</current_state>

<completed_work>

- Fase 0 (Fundação): guard de auth+assinatura em `app/painel/layout.tsx` + migration baseline `supabase/migrations/20260708000000_baseline.sql` — testado em produção (sem sessão → 307, com sessão ativa → 200), commitado (`8dd8bb4`, `bfb1222`), ROADMAP/REQUIREMENTS/STATE atualizados
- Bugs de infraestrutura corrigidos nesta sessão (fora do roadmap, mas bloqueantes): build quebrado (Resend no module scope), hooks do mega-brain-test com timeout errado, domínio de e-mail não verificado, loop infinito no login (magic link usando `action_link`/implicit flow em vez de `token_hash`/`verifyOtp`), projeto Vercel migrado da conta errada pra `thrynclub@gmail.com`, landing page redesenhada (`app/page.tsx`)
- Todo o milestone já foi planejado: `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` (22 requisitos v1), `.planning/ROADMAP.md` (7 fases), `.planning/research/` (STACK, FEATURES, ARCHITECTURE, PITFALLS, SUMMARY)
</completed_work>

<remaining_work>

- Fase 1 (Buscar) inteira: nada foi escrito ainda
  - `01-RESEARCH.md` — não precisa de pesquisa nova de verdade, o essencial já está em `.planning/research/STACK.md` (Places API "New" + PageSpeed Insights + cost breakdown) e `.planning/research/PITFALLS.md` Pitfall 1 (schema place_id-first, NUNCA cachear nome/rating/telefone permanentemente — violação de ToS do Google)
  - `01-01-PLAN.md` e `01-02-PLAN.md` (esboço já existe em ROADMAP.md: 01-01 = integração Places API + schema `leads` place_id-first; 01-02 = UI de busca + lista de leads + guardrail de quota)
  - Implementação: tabela `leads` (nova migration), `lib/google-places/client.ts` (factory function, NUNCA module-scope — já causou um build crash com Resend nesta sessão, não repetir), `app/api/leads/search/route.ts`, `app/painel/buscar/page.tsx`, `lib/quota.ts`
</remaining_work>

<decisions_made>

- Fase 0: migration escrita a partir de introspecção direta do schema (PostgREST OpenAPI), não `supabase db pull` interativo
- Landing page: mantida 100% a paleta/fontes "Signal Ledger" (Toqy/ZapFlow) — ver `.planning/DESIGN-GUIDELINES.md`
- Todos os pushes/deploys agora são na conta `thrynclub@gmail.com` (GitHub `thrynclub-hash`, Vercel `thrynclub-2889`) — NÃO usar mais `leonardomarusso1-design`
</decisions_made>

<blockers>
- Nenhum bloqueio técnico. Só o limite de contexto da sessão atual.
</blockers>

<context>
O usuário pediu explicitamente "pode ir fase 1" antes deste pause. A intenção é continuar exatamente daí — não é preciso re-perguntar escopo, já está tudo decidido em PROJECT.md/REQUIREMENTS.md/ROADMAP.md. Numa sessão nova, `/gsd:resume-work` (ou direto `/gsd:plan-phase 1`) deve seguir sem precisar de mais discussão de produto, só executar o que já foi planejado no nível de milestone.

Atenção pro padrão de "no module-scope client" — apareceu duas vezes nesta sessão como causa raiz de bug real (Resend crashou um build; foi a mesma lição citada em `.planning/research/ARCHITECTURE.md` Anti-Pattern 1 pro cliente do Places/IA). Não repetir na Fase 1.
</context>

<next_action>
Start with: `/gsd:plan-phase 1` numa sessão nova (ou `/gsd:resume-work` primeiro se quiser o resumo automático). Isso vai gerar `01-RESEARCH.md` (pode reaproveitar STACK.md/PITFALLS.md do milestone em vez de pesquisar do zero) e depois `01-01-PLAN.md`/`01-02-PLAN.md`, seguindo o mesmo padrão usado na Fase 0 (escrever RESEARCH.md e PLAN.md diretamente, já que os subagentes especializados do GSD — gsd-phase-researcher, gsd-planner, gsd-plan-checker — não existem como subagent_type registrado neste ambiente; fazer o trabalho diretamente em vez de tentar spawná-los).
</next_action>
